# Superseded — the session-era build prompts

These four files were written for the `docs/session-NN/` structure, which the `D84` rebuild
replaced with `docs/page-NN/`. They point at `docs/session-01/index.html`, which no longer exists,
and at rules that later decisions reversed (`D104` Arabic, `D108` placement, `D115` diagrams,
`D119` structure, `D122` terminology, `D124` one right edge).

**The current prompt is `design/prompts/PAGE_BUILD_PROMPT.md`.** Delete this folder.
