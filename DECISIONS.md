# DECISIONS.md — eCDFP Diploma

**Append-only.** Never edit or delete an existing row. To reverse a decision, add a **new dated row**
that names the ID it supersedes. This file is the answer to "why is it like this?" — if the reason
is not here, it is not a decision, it is a habit.

Source for D1–D10: Part 0 of `00_INSTRUCTIONS.md` (design session, 2026-08-28).

| Date | ID | Decision | Why |
|---|---|---|---|
| 2026-08-28 | **D1** | **6 sessions × 4 h = 24 h.** Locked. | Fixed slot. Everything else is sized to fit it, not the reverse. |
| 2026-08-28 | **D2** | **Hybrid folder model** — flat CEH names + a private `evidence/` folder (the eCIR piece). | Flat is easier to work in; evidence needs a private home that git never sees. |
| 2026-08-28 | **D3** | **Evidence = own lab VM (primary) + public corpora (secondary) + synthesis where valid.** | You cannot fake `.evtx`, E01, memory dumps or hives. Decide once, not per session. |
| 2026-08-28 | **D4** | **6 custom skills**, namespaced `ecdfp-`. Existing `ceh-web-research` and `ceh-chrome-extract` are reused, not duplicated. | One skill, one job. Six is the smallest set that covers the whole build. |
| 2026-08-28 | **D5** | **Site root is `docs/` from day one.** Repo `Ebrahim-Qareen/ecdfp-diploma`, Pages = `main` / `/docs`. | CEH lost two publish rounds renaming `sessions/` → `docs/`. |
| 2026-08-28 | **D6** | **Theme copied from eCIR, not shared.** eCDFP owns `docs/assets/css/ecdfp.css`. | Separate repos. The two sites can diverge without breaking each other. |
| 2026-08-28 | **D7** | **The forensic report template is fixed from Session 1** and required in every session. | Findings-vs-interpretation is the professional skill the course exists to teach. |
| 2026-08-28 | **D8** | **One carry-through case** across all 6 sessions — one incident, acquired in S2, analysed deeper each session. | Fixes CEH's missing continuity at design time instead of mid-course. |
| 2026-08-28 | **D9** | **Outline gate** — a page-by-page outline is approved before any HTML exists. | CEH Session 1 was rebuilt 3× without it; Session 2 was built once with it. |
| 2026-08-28 | **D10** | **Design system ships before Session 1**, not inside it. | Same root cause as D9. Phase 1 is doubled on purpose. |
| 2026-08-28 | **D11** | Phase 0 scaffold creates **only the folders Phase 0–1 need**: `Resources/`, `design/`, `docs/` (+ `assets/css`, `assets/js`, `assets/img`), `testing/`, `tools/`. `knowledge_base/`, `evidence/`, `packages/`, `cases/`, `labs/`, `scripts/` and the Phase 4 `docs/` subfolders are created on first use. | Part 2: "A folder is created on first use." R5: `PROJECT.md`'s folder map lists only folders that exist. Empty folders are litter, and the mount cannot delete. |
| 2026-08-28 | **D12** | `Resources/` is created in Phase 0 rather than at the top of Phase 2. | It is the drop zone for the INE PDFs, which are needed as Claude Project knowledge before Phase 2 starts. It is gitignored, so it never reaches the repo. |
| 2026-08-28 | **D13** | The project instructions file is named **`00_INSTRUCTIONS.md`** at the project root. | Its own footer, Part 13 and `ECDFP_CLAUDE_SETUP.md` all reference that exact path. Renamed from `ECDFP_PROJECT_INSTRUCTIONS_1.md` on 2026-08-28. |
| 2026-08-28 | **D14** | `git init` and its config are run **from Windows PowerShell** (`tools\scaffold.ps1`), never through the device mount. | Part 10: the mount cannot delete, and git's automatic maintenance is the source of `gc.lock` / `maintenance.lock`. `gc.auto=0` and `maintenance.auto=false` are set at init, before git can ever schedule maintenance. |
| 2026-08-28 | **D15** | **Integrated theory→hands-on chunks, closing with one blocked hour** of independent investigation. Per-session shape: ~130 min integrated · 15 min break · 60 min blocked investigation · 15 min hash-verify + chain-of-custody ritual = **220 min**. | Part 12 Q1. The exam is scenario questions against a live lab, so no session can be theory-only and students need the keyboard every ~25 min. But an investigation has a long shape that 20-minute chunks cannot teach — the closing hour is where acquire → verify → analyse → interpret → document actually gets rehearsed. Tier A pages 15–17 are that hour; in Tier B it is the last chapter. |
| 2026-08-28 | **D16** | **Individual work at the keyboard; pairs used only for peer-review of the forensic report.** ~5 students = two pairs and one trio. | Part 12 Q2. The exam is individual and keyboard time is the scarce resource; shared investigations hide who can drive the tools. But findings-vs-interpretation drift is far easier to see in someone else's writing, so review is the cheapest way to teach D7. |
| 2026-08-28 | **D17** | **FOR-WS01 ships as a pre-built clean Windows base VM; students install the tool set and take the `CLEAN-TOOLS` snapshot in S1.** `labs/setup_guide.md` still documents the full build from scratch as reference. | Part 12 Q3. Keeps the part of Part 4's S1 line that teaches something and drops the part that does not: installing Windows teaches no forensics and would consume most of S1's four hours; installing the analyst kit teaches what is in it and where each tool lives, and the snapshot is itself the lesson in known-good baselines. |
| 2026-08-28 | **D18** | **Students pre-download each session's evidence and verify its hash before class; the instructor holds a prepared fallback USB set** in the room. Evidence for session N is published by the end of session N−1, always with MD5 + SHA-256. | Part 12 Q4. Pre-download is the option the instructions flag as the biggest logistics risk, so the risk is bought down rather than accepted: the USB set removes the single point of failure without adding a class-day network dependency. It also makes "verify the hash before you start work" the first ritual of every session, with a mismatch treated as a finding. |
| 2026-08-28 | **D19** | **The carry-through incident (D8) is ONE intrusion chain:** malicious document → malware execution and persistence → C2 beacon → RDP lateral movement with harvested credentials → collection and staging → archive → **exfiltration to USB**, with ransomware tooling staged but never fired. Staged on EVI-SRC01 and imaged in S2. Fictional company and users, no real PII, no real malware sample, documentation IP ranges only. | Part 12 Q5. Every session needs first-class evidence of its own topic and no single-stage story supplies all of it: the document is S3, execution/registry/USB history is S5, deleted staging files and `$MFT` are S4, the C2 pcap and super-timeline are S6. Still one incident, one image, one timeline — which is what D8 requires. Cost: more Phase 3 staging work, mitigated by reusing the eCIR attack scripts and scripting the whole compromise into `evidence/staging_scripts/`. |
| 2026-08-28 | **D20** | **The forensic report is graded every session against ONE fixed 4-criterion rubric**, published in S1 and never changed: (1) Integrity · (2) Method · (3) Findings — fact only, each tied to a named artifact · (4) Separation — interpretation distinct from findings, limitations stated. | Part 12 Q6. Students see the same target six times, which is the only way to prove report writing is improving; a rubric that grows destroys that signal. Criterion 4 is deliberately the thing the course exists to teach (D7). |
| 2026-08-28 | **D21** | **Capstone confirmed: the full investigation (image + memory + pcap) runs in class in S6; the written report is take-home** and submitted afterwards. | Part 12 Q7. A proper forensic report cannot be written well in the last 45 minutes of a four-hour session, and squeezing it would compromise the one deliverable the course is built to produce. Also protects the 220-minute ceiling. |
| 2026-08-28 | **D22** | **The repo `Ebrahim-Qareen/ecdfp-diploma` is PUBLIC.** Pages = `main` / `/docs`. Consequence: the credential and PII scans become a release gate, not hygiene — a finding blocks the push and is never waived. | Part 12 Q8. GitHub Pages publishes from private repositories only on Pro, Team or Enterprise; on Free it is public repositories only. Public also means students open a URL with no account and no access grant. Safe because the separation is structural, not careful: `Resources/`, `evidence/`, `labs/vm_notes/` and `cases/*/answer_key.md` are gitignored, `packages/` is never served, and R9 forbids committing evidence bytes at all. |
| 2026-08-28 | **D23** | **Per-session topic budget is 205 min** (130 integrated + 60 blocked investigation + 15 hash-verify/CoC close), plus the 15 min break = **220**, inside a 240 min slot. `topic_map.md` is built and verified against the 205, leaving ~20 min slack per session. | Part 4's per-session domain hours allocate the full 4.00 h slot, but D15's teaching budget is 220 and the break is not topic time. Without stating the 205 explicitly, the domain hours and the topic minutes silently disagree by 35 min a session. The slack is deliberate — a 4-hour session never delivers 4 hours. |
| 2026-08-28 | **D24** | **Domain balance is reconciled against the coverage matrix percentages, not its raw hours.** Tolerance: ≤ 1.0 pp per domain. Achieved: F 32.4 % (−0.9) · T 27.3 % (+0.2) · P 21.5 % (+0.7) · S 18.7 % (−0.1). | The matrix's hours include the break; its percentages do not depend on it. Percentages are what the exam actually weights, so they are the thing worth holding to. Preservation runs deliberately over because every session spends 15 min on the hash-verify and chain-of-custody close — 90 min across the course — and Part 4 declares the Storage under-weight already. |
| 2026-08-28 | **D25** | **`S5-01` registry structure depends on `S4-07` NTFS `$MFT`; `S6-07` plaso depends on `S6-06` event logs and `S4-08` `$MFT` parsing; `S6-09` capstone depends on all of S1–S5 and introduces no new technique.** These three cross-session dependencies may not be broken by any future re-split. | Hives are files — you cannot locate or carve them without the file system, which is why Windows artifacts must follow file systems. A super-timeline is meaningless until its inputs are understood. The capstone is the arc, not new material. Part 4's ordering rule states the sequence; this row names the specific edges that enforce it. |
| 2026-08-28 | **D26** | **No session required re-splitting.** All six total exactly 220 min. 62 topics, 1230 min of topic time, every prerequisite satisfied earlier in the map, hands-on 155–185 min per session against a 45 min floor. | Part 12 part B requires overflow to be re-split in the map and reported rather than carried into the build. There was no overflow to report. Recorded so a later change that breaks the 220 is visibly a regression against a known-good state. |
| 2026-08-28 | **D27** | **FINDING vs INTERPRETATION is encoded three ways, not one.** `--finding` blue `#7aa2f7` with a **solid** left border; `--interpretation` violet `#c792ea` with a **dashed** left border; `--limitation` grey with a **dotted** border. Each also carries an uppercase chip label, and interpretation body text is italic. | D7 makes this the course's core lesson and D20 grades it, so it has to be readable by every student. Hue alone fails colour-blind students, a bad classroom projector and a greyscale printout. The border style is not decoration — it *is* the lesson: solid = observed, dashed = reasoned. A box shipped without its border style is not the component. |
| 2026-08-28 | **D28** | **eCIR's four accent colours keep their eCIR meanings** — green detection/verified, red attacker/integrity failure, amber warning/anti-forensics caveat, cyan links. The eCDFP pair uses two new hues instead of reusing them. | Students arrive from eCIR carrying those associations. A finding rendered green would read as "safe" and an interpretation in amber as "caution" — the page would say the opposite of what it means. |
| 2026-08-28 | **D29** | **No webfonts.** `--font-body` and `--font-mono` are system stacks only; nothing is fetched from `fonts.googleapis.com` or any CDN. | The lab is offline (Part 6) and evidence is handled on air-gapped VMs. A page that waits on a font CDN renders unstyled in a classroom with no internet, which is exactly where these pages are used. |
| 2026-08-28 | **D30** | **`docs/index.html` renders a card or hub as a link only when its target exists**; unbuilt sessions are non-link cards tagged `Planned`. `ecdfp-publish` converts a card to a link as its session ships. | The Part 9 link check requires every `href` to resolve, and R5's discipline — a document may only claim what is true — applies to the dashboard as much as to `PROJECT.md`. Six dead links on the front page would either fail the gate or train everyone to ignore it. |
| 2026-08-28 | **D31** | **The Part 9 harness is mutation-tested, and the mutation test is re-run after any change to it.** Eight injected defects, all caught, recorded in `testing/README.md`. | A gate that only ever passes proves nothing — which is precisely the `viewportSize` trap Part 9 already paid for. Provenance for the gate itself is the only thing that makes a green result mean anything. |
| 2026-08-28 | **D32** | **`quiz.js`, `homework.js` and `task-creator.js` were written in Phase 1 alongside `session.js`**, though the phase prompt named only `session.js`. | `base_template.html` references all four, and the Part 9 link check fails on any unresolved `src`. The gate the phase requires cannot pass without them. They are deliberately minimal — no storage, no scoring; the assessment of record is the forensic report (D20/D21). |
| 2026-08-28 | **D33** | **The skill set is 8, not 6.** D4's six (`ecdfp-intake` · `ecdfp-evidence` · `ecdfp-session-package` · `ecdfp-session-html` · `ecdfp-case` · `ecdfp-publish`) plus **`ecdfp-pdf-extract`** and **`ecdfp-web-extract`**. Both are **extraction only** — they produce working text and hand off to `ecdfp-intake`, which keeps sole ownership of `knowledge_base/` and `topic_map.md`. | Requested 2026-08-28. The mechanical job (get a source open, cheaply, with provenance and currency checked) is genuinely separate from the editorial job (decide what the course teaches), so R12 holds. The installed `ceh-web-research` and `ceh-chrome-extract` do the same mechanical work but shape their output around the CEH syllabus and an attacker framing, which is wrong for a forensics course — these two land forensics-shaped (artifact · exact path · what it does not prove · parse command · licence) and in eCDFP's paths. Part 11's "do not duplicate the ceh- skills" is satisfied by scope, not by count: do not run both on the same source. |
| 2026-08-28 | **D34** | **On a paid platform (TryHackMe, HTB, CyberDefenders, INE), `ecdfp-web-extract` goes straight to the Chrome browser tools. `WebFetch` is not tried first.** On free, public pages the order is unchanged: `WebFetch` first, browser only when it fails. | Requested 2026-08-28: the TryHackMe subscription lives in the logged-in Chrome session, and the Advanced Endpoint Investigations path is largely premium. `WebFetch` on a premium room does not error — it returns the marketing shell with none of the task content, so a "try the cheap way first" rule silently produces an empty note that looks like a real one. A failure that is indistinguishable from success is worse than no attempt. |
| 2026-08-29 | **D35** | **Browser forensics is IN SCOPE**, at a fixed boundary. **IN:** Chromium `History` (`urls`, `visits`, `downloads`, `downloads_url_chains`) · the Chromium/WebKit timestamp (µs since 1601 UTC) · `transition` core types and redirect qualifiers · `VisitSource` · profile enumeration · Incognito's evidentiary meaning · deleted-row survival in SQLite freelist pages · downloads as four separable claims · **the WebView2 convergence (new Outlook and new Teams)** · defanging as a handling convention. **OUT:** cache reconstruction · cookie/session-token analysis beyond existence · Safari · mobile browsers · extension reverse-engineering · **password-store decryption (R8)**. **Sited in `S5`** (browser as a user-activity artifact, beside LNK/JumpLists/ShellBags/UserAssist), **`S6-09`** (the capstone's phishing → redirect → download leg), and one slide in `S2`/`S4`. Primary tool **Hindsight**; Autopsy *Recent Activity* for the GUI path; raw SQLite for the teaching moment. | Open since room 1 and forced by room 20 (Blizzard), where **3 of 5 questions in one task are browser artifacts** and the room is already on our Priority 2 list. Three further rooms already teach it by accident: Autopsy (room 16) extracts browser artifacts **by default** in an ingest module we teach, rooms 12–14's image is full of `msedge.exe`, and CFReDS (D36) is full of browser evidence. **The decisive fact is the WebView2 convergence:** new Outlook (`olk.exe`) and new Teams both store local evidence in an `EBWebView\Default\` Chromium profile, so declining browser forensics removes not one artifact family but **three — browsing, email and chat** on a modern Windows endpoint. Cost is low (one SQLite DB, one epoch conversion, one flag decode) and the concepts are the same "what it does NOT prove" discipline taught everywhere else. **Consequence, stated honestly: this adds to S5, which is already 65 min overdrawn — it is a further argument for the S5 re-split, not against the topic.** |
| 2026-08-29 | **D36** | **NIST CFReDS is the Tier 2 evidence anchor.** The **Data Leakage Case** supplies **NTFS (PC) · exFAT (RM#1) · FAT32 (RM#2) · UDF/optical (RM#3)** from one scenario, in both **E01 and raw**, with published SHA-1 hashes. **Linked, never rehosted (D22)**, downloaded by students before the session. 🔴 **Its 46-question answer key is instructor-only** and our questions may not be its questions. The **Hacking Case** is the second candidate. | Found in room 16 (Autopsy), which names its own image source. CFReDS is a **US Government work in the public domain under 17 U.S.C. §105**; NIST's terms permit free modification and redistribution with attribution, so linking is unambiguously fine, commercial training use is unrestricted, and rehosting would also be permitted — we link by choice rather than necessity. It closes the two gaps we could not otherwise fill: **we had no exFAT and no UDF evidence at all.** ⚠️ Operational: downloads live on `cfreds-archive.nist.gov` even though the portal is `cfreds.nist.gov`; the PC image is 5–7 GB; the raw variants are **7-Zip split archives (`.7z.001`), not raw-split segments** — extract before adding to Autopsy. ⚠️ NIST's own scenarios appear unchanged since ~2019–2020; treat as stable, not fresh. |
| 2026-08-29 | **D37** | **KAPE is taught for the concepts, and every KAPE step is paired with a free standalone alternative.** Not dropped, not taught alone. | Room 17 surfaced the licence change: KAPE's own FAQ states **"As of January 1, 2026 KAPE IS NO LONGER AVAILABLE for commercial use (i.e. when used on a third-party network and/or as part of a paid engagement)."** Our classroom use is **educational and remains free** — but our students are practising analysts, and we teach KAPE across S4 and S5. Dropping it loses the best available teaching artifact for *"what do I collect and why"*; teaching it alone leaves students unable to do the work on Monday. Pairing costs one extra line per lab step, because the EZ Tools underneath KAPE are separately free and the targets are just file lists. **The licence limit must be stated in the session, not discovered by a student mid-engagement.** ⚠️ Also note the KAPE **core binary is 1.3.0.2 (22 Dec 2022)** while **KapeFiles ships by commit and is current** — "KAPE is stale" is wrong and should not be taught. |
| 2026-08-29 | **D38** | **Linux/macOS forensics stays OUT of scope, confirming `scope_decisions.md`.** Three things are taken from room 22 (ExfilNode) instead, **all at zero new session minutes:** (1) **one cross-platform artifact-equivalence table (figure F1) inside `S1-01`** — not a new row; (2) **the un-examined receiving host becomes an explicitly stated limitation of the carry-through case (D19)**, written into the report rather than left as a gap; (3) **the room-21/room-22 brief-critique pairing becomes S1 homework**, since it needs only published text. Supersedes nothing. If a future INE blueprint puts Linux on the exam, D38 is replaced by a new dated row, not amended. | Room 22 is 100 % Linux across all 14 questions and collides head-on with the existing exclusion. **Adding the topic was rejected on arithmetic:** ext4 internals are an `S4-06`-sized dependency and a Linux artifact block would have to match S5's ten Windows rows — 40–60 min against **D1**'s locked 24 hours, **D26**'s exactly-220 sessions and an **S5 already 65 min overdrawn**; it would also breach **D24**'s ≤ 1.0 pp domain tolerance in Fundamentals and Storage. **Ignoring the room was rejected too**, because it leaves D19's incident ending at *"exfiltration to USB"* with the receiving end never examined. What makes the contrast table worth more than a consolation prize: **every Linux equivalent in the room is weaker than its Windows counterpart** — no `USBSTOR` at all, login records being deleted outright for Y2038 reasons, `.bash_history` undated and silently suppressed by a leading space, no Autopsy module for any Linux persistence artifact. **Read down the Linux column and it is an argument for the Windows one.** That is a better justification for a Windows-weighted syllabus than *"24 hours does not stretch"*, and it converts a disclaimer `scope_decisions.md` already obliges us to make in S1 into five minutes of teaching. Cost is one figure and one homework; **`EVS-10` stays unallocated** and no Linux VM is added to the lab (**D17**). |
| 2026-08-29 | **D39** | **The note verification checker lives in the repo at `testing/verify_note.py`, never in `/tmp`.** Any session running it uses the repo copy. The two controls stay attached to it and are re-run whenever the credential patterns change: **7/7 leak shapes caught, 0 false positives across every real note.** | Found live on 2026-08-29 during room 22's verification: `/tmp/verify.py` on the device was a **stale copy owned by `nobody` and not writable**, carrying the *first* version of the credential check — a literal blocklist (`thm_4n6`, `Passw0rd!`, …) rather than the pattern set. The write of the current checker failed with *Permission denied* and **python3 silently ran the old file instead, which reported PASS.** That is exactly the **D31** failure mode — a gate that only ever passes proves nothing — and it is worse than the `viewportSize` trap because the checker's own output looked identical either way. A blocklist also cannot generalise to a room not yet seen, and **a list of literal passwords would itself be a credential leak in a PUBLIC repo (D22)**. Re-run under the correct checker: 8 artifacts, 6-box consistent, 9 sections, 17 headline assertions present, and both controls green. |
| 2026-08-29 | **D40** | **The six-session carry-through is structured with five devices taken from the Honeynet Collapse module**, all of which cost nothing beyond one figure: (1) **one numbered topology map (figure `F7`), drawn once and reused on all six session pages with the current stage highlighted** — it is simultaneously the scope statement for this session and the catch-up state for a student who missed the last one; (2) **the prior stages' established facts appear in each session as a visibly marked *given context* block**, rendered distinctly by `ecdfp-session-package` and never as prose, so students do not inherit conclusions they did not establish; (3) **at least one session ends on a stated limitation that the next session closes** — the S4→S5→S6 chain, mirroring the module's stage ③→④ handoff; (4) **the carry-through incident's anti-forensic actions are stated in the brief**, and the student is graded on what they establish anyway *and* on what they correctly report as unrecoverable; (5) **`S6-09` contains one strand that turns out to be unrelated to the main intrusion**, graded on whether the student claims a link and on what evidence — both answers acceptable, only an unsupported assertion wrong. ⚠️ **Prerequisite, still open: D19 describes a chain of techniques, not a topology. `F7` cannot be drawn until the incident is expressed as six numbered stages across named hosts, and it must be resolved before any session page is built.** | Extracted from six TryHackMe rooms (`honeynet-collapse-module.md`). **The module's architecture is better than any of its individual questions**, and every device above was verified against its implementation rather than assumed: the shared diagram is served as the *same asset ID* in rooms 23 and 24, so continuity genuinely costs one SVG. Device (2) fixes the module's most consistent flaw — six rooms state the previous stage's conclusion in the same voice as everything else, so a student inherits six conclusions they never established, which is a **D20** criterion-4 habit failure in slow motion. Device (3) is what lets sessions chain without making session five unusable alone: stage ③ proves a connection from the source side and **cannot** prove it succeeded; stage ④ is the destination. Device (4) is the module's best framing decision — declaring the log wipe turns *"find the log entry"* into *"the log entry does not exist, now what"*, which is the real job and which makes a **negative finding** the expected deliverable. Device (5) is the module's final and bravest move, and the one I personally got wrong: I inferred a chaining link between stages ④ and ⑥ from proximity and a shared name, and stage ⑥ refutes it in its own words — *"Not every attack is targeted."* Recorded in `crmsnatch.md` §5.2 rather than deleted. **Rejected from the same module: flat difficulty across six stages (ours ramps — it is a curriculum, not a case), credentials in the task body, and narrating the attack rather than the discovery.** |
| 2026-08-29 | **D41** | **No eCDFP question may have a secret or a data subject's personal information as its answer.** Ask *which* account was compromised, *which* remote was configured, *which* record was staged — **never the value**. Secrets and PII enter the record **by reference only** — path, offset, timestamp, record count — in the restricted appendix. **This binds every quiz, homework, guided lab, case answer key and CTF challenge**, and applies equally to NTLM hashes, Kerberos tickets, API keys, WLAN PSKs and cloud-service passwords, because **all of them are authenticators rather than evidence**. Supersedes nothing; **extends R8** from *"do not reproduce credentials in notes"* to *"do not design questions whose answers are credentials"*. | Three questions in two rooms forced it (`elevatingmovement.md` §4, `crmsnatch.md` §4): one asks for an **NTLM hash**, one for a **cloud-storage password**, one for **a named individual's email address taken from exfiltrated customer data**. **An NT hash is not hash-as-evidence — it is an authenticator**; NTLM consumes it directly and NTLMv2 remains enabled by default (block **L3**), so a published answer key contains a working credential and trains students that submitting one is normal. The PII case is a different and worse category: **exfiltrated customer data is the victim's most sensitive material**, and an answer key containing a data subject's email is a breach of the thing the analyst was hired to protect. **D22** makes this a release gate rather than a matter of taste. 🟢 **And the replacement questions are strictly better forensics**: *"how many customer records were in the staged export, and how do you know?"* exercises archive enumeration (block **N4** — names, sizes, timestamps and CRC32 are readable **without** the password), a CRC comparison against the original file, and a stated limitation; *"what is a customer's email address?"* exercises `Ctrl+F`. **The narrative requirement is preserved without the disclosure** — *"the export contains records for N employees, including the lead developer"* carries the same plot with no PII in the answer key. |
| 2026-08-29 | **D42** | **Currency-block items in `_TOOL_CURRENCY_2026-08-28.md` are always cited as "block R5", never bare "R5".** Bare **R1–R8** always means a project hard rule. The rule applies to every block letter, retroactively and going forward; block **R** is where it first bites, and blocks A–Q are unaffected because no earlier block letter collides. | Discovered 2026-08-29 while writing block **R** for the Priority-3 rooms. The hard rules are numbered **R1–R8** and **R8 is "never reproduce a credential"** — the single most load-bearing rule in the project, cited in nearly every note. A currency block item also called `R8` would make that citation ambiguous in exactly the place where ambiguity is most expensive. **The block was not renamed** (letters are sequential and rooms 30 and 31 already reference "block R" by name); instead the collision is stated in a warning at the head of block R, every reference inside the block was rewritten to the "block Rn" form, and this row records the convention. 🟢 **The cheap fix was available because the collision was caught at the moment of writing, not at the moment of citing.** |
| 2026-08-29 | **D43** | ⚠️ **ARITHMETIC CORRECTED 2026-08-29 — see block R2 and the note below this row; the rule stands, the numbers in the original rationale did not.** **A completion-count curve is a difficulty signal only across rooms of the same access tier, and any use of one must state the tier.** Attrition across a free→premium boundary measures the paywall, not the material. | The Memory Analysis module: **13,179 completions (free intro) → 3,015 (first premium room) → 2,511 → 2,382 → 1,976 (capstone)**. **The single commercial boundary costs 77%; four rooms of rising difficulty cost 34% between them.** This **corrects a method I used on the Honeynet module**, where I read the curve as pure difficulty. 🟢 **That reading survives — all six Honeynet rooms sit inside the paywall, so no boundary confounds them — but it needed this caveat and did not have it.** Consequence: **D40's carry-through difficulty calibration stands on the Honeynet curve alone**, and the Memory Analysis numbers are evidence about TryHackMe's funnel, not about how hard memory forensics is. |
| 2026-08-29 | **D44** | **Detection signatures are taught as structures, never as strings — and at least one taught signature is broken on the following slide.** The worked instance is PsExec: teach `services.exe → psexesvc.exe → payload`, then show Microsoft's own **`-r`** documentation and rewrite it together as *"an unfamiliar service binary spawned directly by `services.exe`, correlated with a remote logon."* | Room 32 teaches the three lateral-movement lineages (PsExec, WMI, WinRM) as flat facts. Microsoft Learn documents **"-r  Specifies the name of the remote service to create or interact with"** — **the service name a detection would match on is an operator-supplied parameter, and the documentation for defeating the signature ships with the tool.** ⚠️ Microsoft **does not state the default name anywhere on that page** (block R5, NOT VERIFIED), which is itself the argument: **the structure formulation needs no default name and the string formulation cannot be sourced.** Figures **F44/F45** are the two slides. 🟢 It also produces a genuine **"cannot be determined"** question — *"was PsExec used on this host?"* — for the tally that closed at **0 of 32**. |
| 2026-08-29 | **D45** | **The public site has two layers on one stylesheet.** A **hub layer** — `docs/index.html`, `docs/roadmap.html` and the hub folders (`lab/`, `cases/`, `resources/`) — and a **session layer**, `docs/session-NN/index.html`, built by `ecdfp-session-html`. Hub pages carry no teaching content, load **no JavaScript**, and may use the FINDING/INTERPRETATION/CANNOT-PROVE components **only where the statement genuinely is one** — the home legend, `resources/report.html`, `resources/evidence.html` and `cases/index.html`. Never on a navigational page (roadmap, lab, references, session briefs). | The hub is where the pair would erode first: it is the page everyone edits, and a violet box used for “look here” costs nothing visibly while destroying the one distinction D7 and D20 are built on. Naming the four pages where the components are allowed makes the rule checkable rather than tasteful — the build gate asserts it, and it caught one real drift during the first build. Hub pages loading no JS also means the Part 9 link check cannot fail on an unresolved `src`, and a classroom machine with no internet renders the site identically. |
| 2026-08-29 | **D46** | **The site skeleton is shared with the CEH and eCIR sites** — sticky top bar with the ITGate mark, hero band with an eyebrow, a two-line `h1` and a stat row, a roadmap card grid, tile hubs, an about row of three cards, and a two-span footer. eCDFP identity is carried by the **cyan accent and the finding/interpretation legend**, not by a different layout. | Students arrive from CEH and eCIR and will open all three sites in the same week; one skeleton means the navigation is already learned before the first click. Read directly from `CEH_Course\docs\index.html` and `ECIR_Course\docs\index.html` rather than reconstructed from memory, so the resemblance is structural (same regions, same order, same component roles) and not a vague family likeness. The three sites still diverge where the subject demands it: eCDFP adds the legend, an evidence hub and a published roadmap that neither of the others needs. |
| 2026-08-29 | **D47** | **Session cards carry no arc or domain colour.** Build state is the only colour signal on a card (cyan rule when a teaching page is live, grey when planned); the hands-on ratio is a **single-fill meter on a neutral track, always directly labelled**; the F/T/P/S split is plain minute chips. The domain colour work happens once, on `roadmap.html`, inside a table with a legend. | CEH stripes its cards by attack arc and eCIR by pillar, and eCDFP cannot copy that honestly. D27 reserves `--finding` blue and `--interpretation` violet and forbids using either for decoration; D28 fixes green as verified, red as attacker/integrity failure and amber as warning. That leaves cyan, which is already links and the brand. **There is no fifth hue available that does not say something false** — a Storage stripe in red or an Interpretation-violet Fundamentals stripe would teach the opposite of the course. A meter needs one hue on a track, so it also avoids a categorical pair that would have to pass a colour-blindness separation check. |
| 2026-08-29 | **D48** | **`docs/session-NN/brief.html` is the student-facing session syllabus and exists from now; `docs/session-NN/index.html` stays reserved for the Phase 4 teaching page.** Home cards link to the brief; `ecdfp-publish` repoints a card to the teaching page as its session ships, and the brief stays. | The alternative — putting the brief at `index.html` now — has Phase 4 silently overwrite it, so the topic list, the prerequisite chain and the pre-class evidence table would have to be rebuilt inside the teaching page or lost. Two files, two jobs: the brief answers “what is this session and what do I bring” and can exist before any teaching content does; the teaching page is the session itself. It also keeps **D30** true without a stub — every card links to a page that exists and says something true, and no card points at an unbuilt teaching page. |
| 2026-08-29 | **D49** | **`itgate-logo.jpg` is copied into `docs/assets/img/`** — never referenced across projects — and is the brand mark and the favicon on both layers. | The same file the CEH and eCIR sites already serve, so the three diplomas carry one mark (D46). Copied rather than linked because `docs/` must stay self-contained with no build step (**D5**) and the classroom is offline (**D29**'s reason): a path into a sibling project would resolve on Ebrahim's machine and 404 for every student. 9.7 KB, no licence question — it is the academy's own mark. |
| 2026-08-29 | **D45** | **Every external lab is opened on its own page and its access tier probed before it enters a catalogue — and the probe is control-tested against a known-paid and a known-free item first.** No tier claim may rest on a search filter, a listing badge, or a previous note. **Re-probe before each intake; tiers change.** | Built while producing `Resources/LABS/`. The TryHackMe probe (test for the "Premium room" badge) was validated against `supplementalmemory` → `true` and `forensicimaging` → `false` **before** any candidate was checked — the same discipline as the render gate's mutation test and the note checker's 7-shape control. 🔴🔴 **It immediately found five of our own tier records wrong**, including one room recorded as Premium that is free, and **the correction invalidated the arithmetic of D43, written the previous day.** A catalogue of what students can access is worthless if it is wrong, and it is wrong by default: the cheapest possible check (a search filter) disagrees with the page often enough to matter. 🟢 Two CyberDefenders labs were also rejected only after being opened — one teaches a Volatility 2 profile workflow that no longer exists, one is reverse engineering mis-shelved as file analysis. **Both looked correct in the listing.** |
| 2026-08-29 | **D46** | **CyberDefenders is the students' primary external practice platform; TryHackMe is the instructor's demonstration platform.** Free labs are assigned; premium TryHackMe rooms are shown on screen under the instructor's voucher and **never assigned**. | Measured, not assumed: **CyberDefenders is 82 free of 255 labs (32%)**, and its free tier reaches `S3`, `S5` and `S6` with full multi-source cases. **TryHackMe's forensics catalogue is the reverse** — 25 of the 32 rooms extracted for this course are premium, and its free tier covers `S3` not at all. 🔴🔴 **The structural counterweight: not one of CyberDefenders' 82 free labs teaches acquisition** — every lab begins with the image already made, the hash already computed by someone else. **So `S2-01`–`S2-05` and `S2-09` stay entirely ours, and `EVS-02` is not optional.** 🟢 That gap is also a free teaching asset: opening any such lab and asking *"who imaged this, and how do you know it is unaltered?"* delivers `S1-07`'s argument using a platform thousands of analysts use without noticing. |
| 2026-08-29 | **D47** | **An external lab is assigned only with its known defect named in the assignment text.** Where the defect is a credential-shaped answer, the affected questions are excluded **and the exclusion is taught as the professional standard it is** (D41), not passed over in silence. | Four of the thirteen shortlisted CyberDefenders labs and three of the fifteen TryHackMe rooms carry a defect a student working alone would absorb: **AfricanFalls and Szechuan Sauce list mimikatz / online hash cracking / hashcat and resolve at least one answer to a recovered credential**; **HireMe instructs the student to open an AD1 image and also lists Autopsy, which cannot open AD1** (block O1); **Sysinternals teaches ShimCache without saying it records presence rather than execution**; **Intro to Cold System Forensics recommends MD5 and SHA-1 for evidence integrity**; **Windows Incident Surface stages a PowerShell profile that clears every event log the moment the analyst opens a shell.** 🟢🟢 **In every case the defect is the better lesson once named** — the HireMe format trap teaches E4/O1/P4 with a live example instead of a warning, and excluding the credential questions teaches D41 more convincingly than stating it. 🔴 **Silence is the only unacceptable option**, because the student meets the defect either way. |
| 2026-08-29 | **D48** | **A tool's licence is checked for the version we ship AND the version one ahead, before the tool enters the student kit.** A capability we decline on licence grounds is recorded as a decision, not left as a gap someone later "fixes". | Third instance of the same trap. **D37** was written when KAPE's FAQ turned out to bar commercial use. This pass found two more: **010 Editor is paid software with a 30-day evaluation and no free tier** — it was sitting in the training Drive ready to hand to students — and **RegRipper 4.0**, where active development has moved, states *"This version is free for personal and academic (college/university) use ONLY. RegRipper4.0 may not be included in vendor products, vendor training, nor in any distribution."* 🔴🔴 **ITGate Academy is paid vendor training that distributes a tool kit — both clauses bite.** Our position: **stay on RegRipper 3.0 (MIT), and record that the 4.0 plugins and ATT&CK mappings are declined on licence, not missed.** 🟢 The generalisable rule is the second half: the trap is not the tool in hand, it is the upgrade a future instructor makes without re-reading the licence. |
| 2026-08-29 | **D49** | **The student tool kit is defined by `design/tools_by_session.md`, not by the `Tools/` folder in the training Drive.** Ship: FTK Imager 8.3 · ExifTool 13.59 · NetworkMiner 3.1 · Arsenal Image Mounter 3.13.368 · **TestDisk 7.2 stable (never the 7.3-WIP beta)** · HxD 2.5.0.0 · OSFMount 3.3.1000 · RegRipper 3.0 · Get-ZimmermanTools · **plus Autopsy 4.23.1, Wireshark 4.6.8 and Volatility 3 2.28.0 with its symbol pack.** Drop 010 Editor and Xiao Steganography. | The Drive kit was enumerated and every entry version- and licence-checked on 2026-08-29. 🔴🔴 **The three tools the course depends on most — Autopsy, Volatility 3, Wireshark — are absent from it entirely**, while a paid hex editor (010 Editor) and a tool with no living vendor (Xiao Steganography, no author site, mirrors dating it to 2010) are present. **The kit is not merely stale; it is mis-aimed.** Five of the twelve tools are also behind by one or more majors — **FTK Imager 4.7.3.81 against a current 8.3** being the worst. ⚠️ **Two traps recorded so they are not re-introduced:** Exterro sells a separate paid *FTK Imager Pro* beside the free product, and **TestDisk 7.3 is still work-in-progress in Aug 2026 — the stable release remains 7.2.** 🟢 Zimmerman's tools install via `Get-ZimmermanTools` rather than a frozen zip, which keeps them current by design. |
| 2026-08-29 | **D50** | **An API that returns "empty" for a shared resource is not evidence that the resource is empty.** Enumeration of third-party storage is confirmed in a second, independent way before any conclusion is recorded — and where it cannot be, the gap is written down as unknown rather than as absent. | The Google Drive connector returned **empty for five of the six subfolders** of the shared "eCDFP training material" folder. **All five were populated** — the Tools folder alone held twelve installers, and the Lectures folder nine session PDFs the connector never surfaced. Had the API answer been trusted, the single most consequential find in the Drive — **that the course was previously delivered in EIGHT sessions, with session 7 split in two** — would have been missed, along with the two sample forensic reports `S6-07` most needs. 🔴 **The residual gap is recorded honestly rather than papered over:** the six `Cases/` subfolders could not be opened by any route tried (connector, double-click, click+Enter, in-Drive search) and after three failed attempts the approach was abandoned; **their contents are marked unknown in `Resources/DRIVE/drive-inventory.md`, not assumed.** |
| 2026-08-29 | **D50** | **The roadmap audit is recorded, not acted on: 20 gaps are declared in `scope_decisions.md` §7 and NO minutes move.** `topic_map.md` stays exactly as D23/D24/D26 define it. The register classes each gap as `decided` (a decision names it, no topic row carries it), `case` (D19 or a case question needs evidence no topic teaches) or `source` (a topic has no INE source). Closing a gap requires a new dated row here, not an edit to the register. | Requested 2026-08-29. `topic_map.md` was built on 28 Aug, **before** the knowledge base was condensed and before D35–D44 were taken, so it cannot contain them — that is drift, not error, and the fix is to say so rather than to quietly re-cut a locked budget. **Acting instead of recording would have cost more than it bought:** `GAP-01` alone (browser forensics, D35) is 35–45 min into an S5 that has none, so absorbing it forces either displacing taught topics or re-splitting the session, which reaches **D1**'s locked 24 hours and **D26**'s exactly-220 arithmetic. Neither is a decision to take as a side effect of an audit. 🔴 **The sharpest finding is `GAP-05`/`GAP-06`: D19 stage 2 is persistence and the incident *ends* in exfiltration to USB, yet no topic row teaches autostart keys, the Services key, or a single USB event ID** — the case asks students to establish two things the syllabus never shows them how to read. `GAP-09` is the same shape: Case 05 asks *which user* and nothing maps a SID to a name. Those three are worth more than the twelve `source` rows combined. 🟢 The register is **published** — `roadmap.html` renders it and every session brief shows its own open items — because a gap the students' own page admits is one nobody can forget, and R5 applies to the site as much as to `PROJECT.md`. |
| 2026-08-30 | **D51** | **A figure may animate when the animation shows a mechanism over time — and only behind an explicit play/replay control.** Autoplay and looping stay forbidden. Animation is hand-authored inline SVG (SMIL `begin="<id>.click"`), never a GIF, a video or a diagram library. | `design_system.md` §8 rule 7 exists so that nothing moves while an instructor is talking over the page; a click-to-play animation satisfies that intent while letting a figure show change over time, which is the one thing a static diagram cannot do. A GIF was requested and declined on three grounds: it breaks §5 (*inline SVG only, no image files for diagrams*), it is ~2 MB against ~8 KB, and it bakes in a background so it cannot read correctly in the dark theme. Three S1 figures use it — `S1-F4` (volatile stores draining in proportion to survival time), `S1-F7` (the avalanche), `S1-F11` (a write reaching the media on one path and being dropped on the other). ⚠️ **Implementation note that cost time:** SMIL cannot interpolate a CSS `var()` — animating `fill` to `var(--accent-red)` renders white. Cross-fade a second shape instead, which keeps the tokens. |
| 2026-08-30 | **D52** | **Every in-page knowledge check and every quiz question is multiple choice.** Supersedes `design_system.md` §4.2, which required each knowledge-check page to mix `.q.mcq` with `<details>` free recall. | Ebrahim's call, 2026-08-30. **The cost is named rather than hidden:** MCQ tests recognition and free recall tests production, and writing a clean finding is a producing skill — so removing free recall from the checks removes the only place students *produce* one before the report is graded. Mitigated deliberately: the two rewrite tasks stay as written work in `student_activity.md` (they are activities, not checks), and `D20` criterion 3 grades production in the report every session. S1 went from 11 MCQ + 12 reveals to **23 MCQ**, and `quiz.md` from 6 MCQ + 4 short to **10 MCQ**. |
| 2026-08-30 | **D53** | **`ecdfp.css` gains section 15 — a session-layer revision.** Larger heading scale (`h1` to 3.1 rem, `h2` with a cyan left rule), `--content-max` 1000 → **1320 px**, `--sidebar-w` 260 → 240, the page kicker rendered as a state pill, `mark`/`.key` as an inline amber highlight, inline `<code>` on a cyan tint, tables as rules rather than boxes, and two new components: **`.split`** (content beside a narrow sticky column, collapsing to one column under 900 px) and **`.gui`** (a labelled mock window — title bar, dots, mono body with prompt/ok/no/dim classes). | Requested 2026-08-30: bigger titles, content filling more of the page, more colour on the parts that carry meaning, and a small visual beside the content giving a GUI view. `.gui` is **explicitly a labelled illustration, never a screenshot** — it renders a command and its real output as styled text, so it is honest, weighs nothing, scales on a projector and needs no capture. It is what let `S1-06` show the real `sha256sum -c` output — 3 `OK`, 1 `FAILED` — before the screenshots exist. Five panels in S1. `design_system.md` §2, §4.4 and §7 updated in the same pass so the system stays the single source of truth. Render gate re-run after the change: **PASS, zero findings at all five widths.** |
| 2026-08-30 | **D54** | **`EVS-01` is Tier 1, generated by `scripts/make_evs01.py`, and verified.** Four UTF-8 plain-text documents (6 144 B) plus `EVS-01.sha256` and `EVS-01.md5`, published in `docs/session-01/`. One file — `EVI-SRC01_acquisition_log.txt` — is altered by **one character** after the manifest is computed. | Part 8 step 0 was failing: `design/evidence_sets.md` did not exist and S1 could not be taught. Text was chosen over an image deliberately — S1 needs no forensic software, so the evidence should need none either, and a deterministic generator (fixed seed `20260304`, fixed base date) means a student's copy is byte-diffable against the instructor's. **The altered file is the acquisition log because its content *is* the integrity claim** — it is where the word `verified` appears, and `verified` means the tool compared its own write to its own read-back. Verified by running it, not by asserting it: `sha256sum -c` → 3 `OK`, 1 `FAILED`; `md5sum -c` the same; regenerated to a second path and diffed byte-identical. Contains no real personal data, no real company, no credential and **no IP address at all** (`D41`, `R8`). `R9` holds — the repo carries the manifest, the generator writes outside the tree. |
| 2026-09-04 | **D55** | **The Chain-of-Custody record is a cumulative per-session deliverable at `docs/session-NN/record.html`, generated by `scripts/gen_session_record.py`, and its structure follows SWGDE 18-Q-002, SWGDE 18-F-002 and ISO/IEC 27037 rather than a shape of our own.** One shared merge-safe browser key (`ecdfp-case-file`) carries entries forward across sessions; the custody-transfer log and the disposition are **case-level and are never cleared with a session**; the exported HTML/PDF is the record of authority and says so on its face. | The CEH cumulative team-report proved the engine, but a forensic record has to answer to published standards or it teaches a habit no court recognises. Mapped from the sources: **SWGDE 18-Q-002 §5.1–5.7** supplies the skeleton — general information · request and authority · item identification · results · opinions **with their basis** · **disposition of originals and derivative works** · **authorization and signature**. 🔴 **The last two were absent from the first build** and are now present, the signature as a printed line on the export. **SWGDE 18-F-002** sets the custody minimum: unique identifier, date/time of receipt, and every transfer identifying each person by **name and signature**. **ISO/IEC 27037** supplies the phase language (identification → collection → acquisition → preservation) and the rule the whole record exists to satisfy: **every action must be reconstructible by a qualified third party**. 🟢 Steps and examples are per-topic from `topic_map.md` — S2 records the write blocker *before* the source is connected, S4 cites an `$MFT` record number, S5 a registry key path, S6 a frame number and the measured clock skew — while the spine (authorization gate → topic steps → custody entry → hash-verify and close) never changes, which is what makes six sessions one document. ⚠️ The gate warns when an **active** step is ticked before authority is signed; **Clear** erases only the current session, never a prior one or the case log. Verified: **6 pages × 6 widths force-expanded**, plus gate, true S1→S2 carry-forward, cumulative export and clear-scope — all clean. |
| 2026-09-06 | **D56** | **The Linux acquisition host is the instructor's existing Kali VM, run for `S2-07` from a CLEAN snapshot taken before the F7 staging — there is no separate `FOR-LNX01`.** Rename `FOR-LNX01` -> Kali wherever it appears (`topic_map.md`, `00_INSTRUCTIONS.md` Part 6, `docs/lab/*`, `docs/roadmap.html`, `docs/session-02/brief.html`, `tools_by_session.md`). `dc3dd 7.3.1` and the Sleuth Kit run there. | The 3-VM lab that was actually built (see the lab build notes) has no `FOR-LNX01`; the built machines are FOR-WS01, EVI-SRC01/FIN-WKS-07 and one Kali. F7 also makes the *running* Kali the compromised receiving host, declared **not examined** (`D38`). Acquiring evidence in `S2-07` from that same compromised, running host would be a contradiction a student spots. A snapshot rolled back to before staging gives a clean, uncompromised acquisition workstation while still using one VM. |
| 2026-09-06 | **D57** | **Session 2 acquires the D19 carry-through incident from the built victim (exhibit `EVI-SRC01`, Windows host `FIN-WKS-07`), found running: live response + memory (`EVS-03`), then power-down and disk image E01+raw (`EVS-02`), plus the suspect USB (`EVS-04`) and a KAPE/triage set (`EVS-09`); attack window 24-26 Aug 2026, acquired ~1 week later.** `EVS-01` / Case 01 (the powered-down seizure, no memory captured, dated 2026-03) is **kept byte-for-byte** as the standalone integrity-and-custody exercise and becomes S2's cautionary contrast. Memory **is** captured here, so `EVS-03` exists for `S6-09`/`S6-10`. | Chosen over retiming `EVS-01` (which is published and verified, `D54` -- reissuing its manifests is the costly path) and over a fourth VM. Keeping S1 intact honours the "make the loss the lesson" call: the volatile data a cold power-down destroys is exactly what `S2-01`/`S2-02` teach, and the S1 record is the worked example of that loss. It also matches what is **already built**: `gen_session_record.py`'s S2 data shows the host found powered-ON with volatile capture first, and the victim VM's ComputerName is `FIN-WKS-07`. Open for confirmation at the outline gate: Case 01 is framed as a standalone warm-up seizure, the D19 incident as the graded carry-through case. |
| 2026-09-06 | **D58** | **The roadmap is re-cut so examination starts in Session 1 and no idea is taught twice.** `S1-02`'s order-of-volatility is **deleted** (owned by S2, taught once and operationally); S1 theory trimmed to 10 min a block and S1 gains a **22-min hex / magic-bytes** block; `S2-01` is **deleted**, physical-vs-logical and image formats are **merged**, and S2 gains a **20-min signature-vs-extension** block; **S3 is rebuilt as *Hidden Information*** — EXIF deepened plus **new steganography (25), embedded/appended data and polyglots (20) and image forensics (20)**; `S3-06` is absorbed into S4's slack block and MFTECmd folded into the NTFS block. Every session still totals **130 + 60 + 15 = 205** (`D23`, `D26`) and `D1`'s 24 hours is untouched. **An anti-repetition rule now applies: each idea is owned by exactly one block and afterwards only referenced, never re-explained.** | Instructor review of the built S2 page. Measured, not asserted: order of volatility was taught twice for 35 min; "memory comes first" appeared in six places inside S2 and "`verified` checks the tool's own output" in eight; **S1+S2 held 97 of the course's 210 pure-lecture minutes (46 %)**, so a student first met hex at hour 9 of 24; and **steganography had zero rows** despite `binwalk`, `zsteg`, `stegsolve` and `steghide` sitting in the knowledge base. ⚠️ **One honest consequence: `S4-07` file carving is reclassified `T` → `S`.** Without it the re-cut breaches `D24` (T +1.8, S −1.3); with it every domain lands inside 1.0 pp (F −0.5 · T −0.2 · P −0.1 · S +0.7). Carving recovers data from storage structures, so `S` is defensible — but the reclassification was made to hold the balance and is recorded here rather than hidden in the table. |
| 2026-09-06 | **D59** | **The roadmap is rebuilt against all three source bodies at once, `D1`'s 24 hours held.** Session 6 stops carrying four INE units: the **capstone leaves session time** and becomes graded take-home **Case 06**, released at the close of S5 and debriefed in `S6-12` (20 min); **Windows event logs + SID→name mapping move to S5** as `S5-08` (14 min); and **INE §7.2\* TCP/IP (135 pp) is declared explicit pre-work** rather than silently assumed. S6 is retitled *Network Forensics, Logs, Timelines & Reporting* and gains `S6-06` network attack patterns (12), `S6-07` internet artifacts — browser + email (20) and `S6-10` log analysis — web/syslog (25 INV). **`S3-05` executable analysis is restored at 25 min**, funded by trimming endianness 15→10 and image forensics 20→12 and by merging steganography with embedded/appended data into one 33-min block. **`S2-07` HPA/DCO (8 min) is new.** Every session still totals 130 + 60 + 15 = **205** (`D23`, `D26`), verified by generating `topic_map.md` from data rather than editing it. Supersedes nothing in `D58`, which it keeps whole. | A full review of the three source folders, audits kept in `design/review/`. **The defect was measured, not felt.** Sessions 1–5 carried one INE unit each; S6 carried four — U7 Network (317 in-class pp), U8 Logs (87), U9 Timeline (51), U10 Reporting (61) — 516 pages and the capstone in 205 minutes, a rate of **2.43 pp/min against a 1.0 benchmark**. 🔴 **The previous instructor spent 462 minutes on that same material** (his S6 200 + S7 194 + S8 68). We were asking one session to deliver more than twice what it can hold, and the 8-session proposal of 2026-08-29 did not fix it either — it moved S8 from 2.46 to 2.43. **Total volume was never the problem:** his eight sessions carry ~1,176 evidenced minutes against our 1,230. 🟢 The three moves are each independently justified — the CTF he promised was never built so it gains from being real take-home work; the event-log block sits where its artifacts already live and closes `GAP-09` (Case 05 asked *which user* and nothing mapped a SID to a name); and TCP/IP is genuinely pre-known from CCNA → SOC → eCIR, which is why his own network session ran ~110 min of revision. ⚠️ **Eleven INE sections had no home, not the seven previously recommended** — 238 pages, including **§3.7.4 Executable Analysis at 69 pp, the largest single section INE ships**, which `D58` dropped. All eleven now carry a row; `6.3.5` Libraries and `6.3.6` Search History are dropped on the record as superseded by shellbags and browser history. ⚠️ **Three reclassifications hold `D24`** and are recorded rather than hidden, following `D58`'s precedent with file carving: `S3-05` `F`→`T` (static PE analysis is a tool chain), `S6-06` `F`→`T` (reading attack traffic is a technique), `S6-12` `F`→`T` (the capstone it replaces was already `T`). Without them the rebuild breaches at F +4.1 / T −4.6; with them every domain lands within **0.6 pp** of the exam blueprint. |
| 2026-09-06 | **D60** | **Nine delivery rules from the THM harvest become binding on every session build**, recorded in `design/session_blueprint.md` §3: order never inverts (concept → location → manual → tooled → abuse → applied → unguided); every artifact gets six moves with **"what it does NOT prove" and the caveat written first**; open with a reported symptom, never an artifact name; manual before tool, always, with the reason stated; one question, one artifact, one answer, sequenced as a narrative; answer format stated every time; a stem says what to find, never why it happened; **one "cannot be determined" question per session**; and where two artifacts record one event, the disagreement *is* the finding. Difficulty ramps by removing scaffolding, never by relabelling it — every lab carries a one-line harder mode. | 33 room extractions reviewed. The rooms are the strongest delivery model available to us and the pattern is consistent enough to state as rules. 🔴 **The previous material fails most of these and the failures are measurable:** six challenges with **zero answer keys** (three distributed by file-host links that are probably already dead), a *Reporting & CTF* deck containing no brief, evidence, questions or scoring, **six phantom labs** announced and never shown, and fifteen registry keys opened in S04 with no conclusion stated. 🟢 **The "cannot be determined" rule is ours, not THM's** — zero of the 33 rooms ask one, ~60 candidate questions are documented in the harvest, and it is the cheapest way to make `D7`'s findings-vs-interpretation split operational instead of stated. 🟢 **Reporting is the clearest differentiator we have:** no THM room writes a report, defines findings vs interpretation as a deliverable, or grades reasoning — which is why `S1-04` (25 min) opens the course with the template and `S6-09` (20 min) plus the every-session ritual close it, rather than reporting being one block at the end. |
| 2026-09-06 | **D62** | **`.gui-title` gets `min-width: 0` + `overflow: hidden` + `text-overflow: ellipsis` + `white-space: nowrap`.** A long title now truncates instead of widening its flex row. Put the identifying part of a filename first, because the tail is what gets cut. | Found by the Part 9 render gate on the rebuilt S1 page: `EVI-SRC01_acquisition_log.txt` and `$ xxd -l 16 notes.txt` both pushed `.gui-bar` past the viewport at **480 px**. 🔴 **The tempting fix was to shorten the label on the page, and it was the wrong one** — a filename in a `.gui-title` is the natural thing to write, every session will do it, and a per-page workaround means the defect returns in S2 and is found again by the same gate. `.gui-dots` already carries `flex: 0 0 auto`; the title simply never got the matching `min-width: 0` that `.page-layout > *` gets for the same reason. Fixing the system once costs one line and removes a whole class of finding. |
| 2026-09-06 | **D61** | **The session page is a visual teaching surface, not a textbook, and is measured against five checks** (`design/session_blueprint.md` §3.8, enforced by `scripts/density_gate.py`): every teaching page carries at least one visual; never more than three consecutive `<p>`; **average ≤ 180 visible words per page and no page over 250**; a concept is explained on exactly one page and only *used* elsewhere; and the text is the caption for the visual, not the reverse. **The prose that used to sit on the page moves to `student_guide.md`**, which is where a student re-reads it alone. Session 1 is rebuilt against this. | Instructor review of the built S1 page, then measured. 🔴 **The old S1 page carried 11,111 visible words across 24 pages — 462 per page, worst page 1,160, five pages with no visual at all, and 262 paragraphs against 12 diagrams.** That is ~56 minutes of reading inside a session whose teaching budget is 130 minutes. The concept counts show what the words were spent on: **"hash" appeared 86 times, "md5" 35, "interpretation" 26 — while "magic bytes", a 22-minute hands-on block, appeared once.** 🟢 **The diagnosis is structural, not stylistic:** the page was doing two jobs, teaching surface and textbook, so one idea got restated in eight places while the practical anchor got a single mention. The document set already separates those jobs — the page projects, `student_guide.md` is read alone, `instructor_guide.md` carries the script — and the fix is to hold that line. ⚠️ **The rebuilt S1 measures 2,912 words, 132 per page, worst page 190, zero pages without a visual, zero four-paragraph runs**, and passes the Part 9 render gate at all five widths. The gate was then run against the untouched S2 page and **failed it on six checks**, which is the evidence that it discriminates rather than merely passing (`D31`). |
| 2026-09-06 | **D63** | **Four ordering defects fixed in `topic_map.md`, and the build prompts are split into a shared contract plus one prompt per session** (`design/prompts/SHARED_RULES.md`, `S1_BUILD_PROMPT.md`, `S2_BUILD_PROMPT.md`; the old `session-01-build.prompt.md` is superseded). The fixes: **(1)** `S2-09` Case 02b is rescoped from *examine the partition and file-system structure* to **verify the acquisition is complete** — volume present, sector count, reported file-system type; **(2)** `S2-07` HPA/DCO moves **before** the imaging tools, beside acquisition scope; **(3)** `S1-05` becomes toolkit **verify** + snapshot, with installation declared pre-work; **(4)** `S1-04` the report template moves after hashing, custody and write-blocking in the teaching sequence. Row order in the S1 and S2 tables is now teaching order and says so; the `SN-NN` labels stay fixed because ~90 files reference them. | Asked directly whether S1 and S2 were correctly sequenced. A prerequisite scan came back clean — **all 66 prerequisites are satisfied earlier in the map** — so the defects were not in the declared dependencies but in the *source* units the rows draw on. 🔴 **`S2-09` was the real error: it asked students to examine MBR/GPT/FAT/`$MFT` structure, which is INE Unit 3 and is taught in Session 4 — two sessions later.** Acquisition *verification* is a Session 2 skill and *analysis* is a Session 4 skill; the row conflated them. 🟡 `S2-07` taught "there is storage your image did not capture" **after** teaching how to image, which inverts the reason for knowing it. 🟡 `S1-05` said *install*, which spends 15 classroom minutes on progress bars; the original intent (`session_map.md`) was *verify + snapshot*, with installation as pre-work. 🟡 `S1-04` sat at row 4, teaching students to write a finding before they had produced one — the built page already delivers it at page 15, after the student generates a real finding by hand, so the map was simply out of step with the page. ✅ Three other forward references were checked and are **deliberate, not defects**: `S1-09`/`S2-06` (hex, signature-vs-extension) were moved early by `D58` so examination starts on day one and `S3` is built on top of them, and `S5-08`/`S3-06` are `D59` moves. Coverage re-verified after the fixes: **34/34 of the previous instructor's demonstrated topics, 10/10 INE units, 12/12 orphan sections.** |
| 2026-09-06 | **D64** | **`ecdfp.css` gains section 16.0 — section 15's two responsive breakpoints are re-asserted, each scoped to its own media query.** Section 16 re-declared `.page-layout { grid-template-columns: var(--sidebar-w) minmax(0,1fr) }` and `:root { --sidebar-w: 300px }` **after** section 15's media queries, at equal specificity and later in the file, so it silently cancelled both. | 🔴 **Below 900px the sidebar never collapsed.** At 480px the grid resolved to `300px 140px`: every session page rendered its content in a **140px column** beside a full-height sidebar, with headings clipped. At 900–1099px the sidebar stayed 300px instead of the documented 210px (`design_system.md` §7). **The Part 9 render gate reported PASS throughout** — nothing overflowed, because the content was *crushed, not spilled*, and the gate measures overflow. Found by screenshotting S1 at 480px during the `D51` animation pass and looking at it. This is precisely the trap `19.1` documents (*"a rule appended after section 15 at equal specificity silently wins"*) — and the project note for `19.1` already said **section 16 has this bug against section 15**; it was recorded and not fixed. Now fixed at the source: two scoped lines beside the offending declarations, so the whole class of finding goes away for all six sessions rather than per page. Re-verified after the change: 480/700 single column with the sidebar above main, 900 back to a 210px sidebar, render gate **PASS, zero findings** across every built page at five widths. |
| 2026-09-06 | **D65** | **Two `ecdfp.css` inline-content fixes: `.steps > li` becomes a block with an absolutely positioned number (§16.4 rewritten), and inline `<code>` gets `white-space: nowrap` with a break-again fallback under 700px (§16.6).** | 🔴 **`.steps > li` was `display: grid` with a 32px number column, so every child ELEMENT became its own grid item.** A bare `<code>` or `<strong>` landed in the 32px column and wrapped **one character per line** — `FOR-WS01` rendered as F/O/R/-/W/S/0/1 and the row grew from 51px to **252px**. The same defect had already been patched twice at the symptom — `.steps > li > b { color }` first, then `.steps > li > span { display: block }` — and the next inline tag broke it again both times, which is the signature of fixing an instance instead of the class. The number is now an absolutely positioned marker and the item an ordinary block, so `<code>`, `<strong>`, `<b>`, `<em>`, `<mark>` and `<a>` all flow as inline text. 🟡 Separately, identifiers in this course are full of hyphens — `CLEAN-TOOLS`, `FOR-WS01`, `EVI-SRC01`, `SHA-256` — and a hyphen is a normal line-break opportunity, so a chip landing at a line end split into `CLEAN-` + `TOOLS`, each drawing its own bordered chip: it read as two different identifiers. ⚠️ `word-break: keep-all` was tested and does **not** suppress hyphen breaks in Chromium; `white-space: nowrap` does. **The render gate then caught the cost of that rule** — an 86-character command sitting in an inline `<code>` on p9 overflowed the viewport at 1100 and 900px. That was the correct finding: a command that long is a block, not a chip, and it is now a `<pre>`. Both gates re-run clean afterwards. |
| 2026-09-06 | **D66** | **`data-task-counter` moves off the `<ul>` onto its own element, the pre-written homework tasks gain checkboxes, and `homework.js` refuses a counter that contains the list.** | 🔴 **The homework list was being deleted at runtime on every built session page.** The markup was `<ul id="taskList" data-task-counter>`, and `homework.js` does `counter.textContent = done + ' / ' + items.length` — where `counter` resolved to the `<ul>` itself, so the write replaced all five `<li>` children with the string `0 / 5`. Students saw a heading, an empty gap and an *Add a task* button; the homework was invisible on S1, S2 and S3. Separately, only student-*added* tasks got checkboxes — the pre-written ones were bare `<li>` text, so the counter could never move off zero even when the list did render. Every gate passed throughout: the density gate reads the **source**, where the `<li>` items are present, and the render gate checks overflow and console errors, not whether content survived a script. Found by screenshotting p21 after adding the homework labs. Fixed in the markup on all three pages and hardened in the shared script, so a counter that is (or wraps) the list is now refused rather than allowed to eat it. |
| 2026-09-06 | **D67** | **`EVS-01` is published for download at `docs/session-01/` — `EVS-01.zip` plus the loose `EVS-01/` folder — and `report_template.md` is served beside it.** A new page 22, *Write the report*, carries both links and maps each of the ten report sections to what Session 1 already produced. | `D18` requires students to verify the evidence **before** class, and `D54` had left the generator writing outside the tree, so there was **no published route to the files at all** — the page linked neither the evidence nor the template. `R9` is not breached: its prohibition names evidence **bytes** — *image, memory dump, hive, raw pcap* — and the gitignore's byte-format list is exactly those extensions. `EVS-01` is 6 144 B of UTF-8 text we authored, Tier 1, with no personal data, no credential and no IP address (`D54`, `R8`, `D41`). It was already fully derivable from the public repo, because `scripts/make_evs01.py` is committed and deterministic — publishing adds convenience, not disclosure. Verified after publishing: `sha256sum -c` and `md5sum -c` both give 3 `OK` / 1 `FAILED` against the served copies, and the two manifests in the set are byte-identical to the ones already at `docs/session-01/`. ⚠️ **Noted, not fixed:** that same committed generator states the alteration in clear at lines 155–156, and `design/evidence_sets.md` states it in prose — so **Case 01's answer is already public** and was before this change. The exercise survives it: homework Lab 1 grades what a student can *prove* from the manifest, and "one character was changed" is marked **down** as unsupported whether or not they read the generator. |
| 2026-09-06 | **D68** | **Five acquisition tools added to `design/tools_by_session.md` §S2: BriMor Live Response Collection, Velociraptor, WinPmem, DumpIt and `hdparm`.** | `SHARED_RULES` §10 forbids introducing a tool the course has not declared, and `S2_BUILD_PROMPT` §6 requires the collector to be **named** — the old build said *"run the live-response collector"* and never named one, which is the defect it was written to fix. The two documents therefore contradicted each other until the tools were declared. Recorded with the catch that matters for each: BriMor is free but **not licensed for commercial use**, so Velociraptor (Apache 2.0) is named alongside rather than instead; a WinPmem capture smaller than installed RAM is **truncated, not compressed**, which is the completeness check and the S2 quiz answer; and `hdparm -N` is run **before** the imager, never after. |
| 2026-09-06 | **D69** | **Session 3 is resequenced into two finished threads, and its build prompt is written** (`design/prompts/S3_BUILD_PROMPT.md`). Order becomes `S3-01` endianness → `S3-02` EXIF → **`S3-06` image forensics** → `S3-03` hidden data → `S3-04` malicious document → `S3-05` executable analysis; `S3-06` previously sat last, after the document and executable blocks. Minutes unchanged at 130. **`EVS-06` is confirmed absent**, and pages 13/14 are to be taught on Tier 3 samples the build authors — a `.docm` with a visible macro and a benign compiled PE — rather than stalling the session; only the page-17 carry-through investigation stays provisional. | Writing the prompt forced a read of the actual row order. 🔴 **The image thread was being interrupted by two document blocks.** EXIF, ThumbCache and LSB steganography are one continuous argument about images, and `EVS-10`'s `invoice_batch.jpg` is the hinge between the first two — its EXIF *is* the metadata lesson and its surviving thumbnail *is* the image-forensics lesson. Teaching OLE/OOXML and PE headers in between broke that hinge for no gain. Documents and executables are the second thread and are themselves paired, because `S3-05` reads the binary that `S3-04`'s document drops. 🟢 **`EVS-05` and `EVS-10` were found already built and verified** (generators `make_evs05.py` / `make_evs10.py`, real hashes published) — the summary table in `evidence_sets.md` still says *not started* and is **stale**. Unlike S2, **S3 is not evidence-blocked**, and the prompt carries the real filenames and byte values so nothing is invented. ⚠️ `EVS-06` genuinely has no section, because it was to be carved from the unacquired `EVS-02`. Authoring a macro-bearing document and a benign binary sits inside the Tier 3 bar, which forbids fabricating `.evtx`, E01/AD1/raw, memory dumps and registry hives and explicitly permits authored media — `EVS-10`'s own header makes that argument. 🔴 **Three defects in the previous instructor's session-2 deck are written into the prompt as blockers, not notes:** real PII across ten screenshots (his profile path, working tree, drive models, an FTK case path and **a live Windows SID**), two graded tasks depending on Google Drive links, and **Xiao Steganography — no vendor, no licence**, which is why steganography was dropped from the roadmap once already. |
| 2026-09-08 | **D70** | **`D1`'s 6 x 4 h = 24 h stays the design target, not a hard ceiling. Up to two extra sessions may be added during the build - maximum 8 x 4 h = 32 h - as a relief valve, never as planned space.** An extra session is opened only when a named session breaks the 220-min ceiling and re-splitting inside the existing six cannot fix it. Whatever moves out must be **one coherent block** with its own title and its own case, never leftovers swept into a spare slot. The call is made **no later than the S5 build**, so the schedule is fixed before students are told, and it re-runs the `D24` domain reconciliation. | Ebrahim owns the slot, so the hours are not externally fixed - but designing for eight up front would repeat the defect the `D59` review measured: volume expands to fill whatever space it is given. Holding six as the target forces every session to earn its minutes; holding the valve open stops a real overflow from being paid for by cutting content the exam needs. |
| 2026-09-08 | **D71** | **The session's internal shape is rebuilt: 70 theory + 15 break + 60 guided lab + 60 independent investigation + 15 report/integrity close = 220. Supersedes `D23`'s 130-integrated / 60-investigation split; the 205 topic-minute total is unchanged.** (a) The break sits at **minute 70** - inside the first two hours, on the theory-to-practice seam. A **second break is the instructor's call at delivery time and is not budgeted in the material**. (b) **Theory is deliberately the smaller half.** The concept is laid down once in Block A and taught **again, in context**, inside Block B. (c) Theory and practice are cross-referenced both ways: every theory topic closes with where it will appear in the lab; every lab step opens with the theory it applies. (d) **The report is written inside B2, not after it** - Block C is finishing it, plus hash-verify and chain-of-custody sign-off. (e) Students work at their own keyboard during Block A demos, so hands-on does not fall. | Ebrahim's own delivery model, stated 2026-09-08: he covers the theory a second time while running the practical, and that repetition is what makes it stick. A 100+ minute theory front-end before anyone touches a keyboard also does not match an exam that is 15 scenario questions answered against a live VPN lab under time pressure. The second teaching pass costs no extra minutes because it rides inside lab time that already existed. |
| 2026-09-08 | **D72** | **The course is taught on two axes at once - DF as a skill every SOC analyst needs, and DF as a separate profession someone may specialise in - and the student path is corrected to the real seven-course track (CCNA -> MCSA -> Linux Admin -> SOC -> CEH -> eCIR -> eCDFP; `00_INSTRUCTIONS.md` Part 1 wrongly said 'fourth course').** The carry-through case (`D19`) is presented as the forensic phase of an incident of the kind they handled in eCIR: *you contained it, now you prove it* - stated once, in `S1`. Three mechanisms carry it and no more: a course-level capability page (SOC use vs examiner deliverable, ~12 rows), a per-session **opening bridge** (<=3 lines) and **closing 'what you can now sign'** (<=4 lines), and a selective one-line `SOC` chip at topic level. **Linking is allowed; re-teaching is not** - a bridge names prior knowledge and moves on. `R10`'s six-box artifact template is unchanged; the framing never becomes a seventh box. | Ebrahim's framing, stated 2026-09-08: DF is one of a SOC analyst's skills, but he wants it taught as a job, with students always able to see what each part buys them in their current role. Naming both axes explicitly stops the two failure modes - a course that reads as SOC tooling and never reaches examiner standard, or one that teaches courtroom procedure to people who will spend next year triaging alerts. The size caps exist because he asked for the linkage to stay light: the depth belongs to DF, and a forced bridge on every topic trains students to skip all of them. |
| 2026-09-08 | **D73** | **The eCIR bridge is literal, not thematic. The carry-through case (`D19`) fixes its entry vector as a phishing email carrying the malicious document, over an HTTP C2 channel.** In eCIR the students worked web, network and email attacks; two of those three families become the same evidence seen from the disk side - the phishing message and attachment recovered in `S6-07` (email forensics), the C2 beacon in the `EVS-08` pcap and the host's own artifacts, and the HTTP delivery plus browser artifacts also in `S6-07`. The story therefore **closes**: the last session recovers the first artifact of the incident. | Asked 2026-09-08 whether a real eCIR case could be continued by name; the answer named the three attack families rather than one case. Fixing the delivery vector is what turns a framing device into an actual handoff, and it costs nothing - email and browser forensics were already `S6-07` (`GAP-01`/`D35`), and the pcap was already `EVS-08`. It also gives `S6` a genuine ending instead of a capstone that merely stops. Binds `EVS-02`: the disk image must contain the mail artifact, so the staged compromise script has to deliver by mail. |
| 2026-09-08 | **D74** | **The academy does not provide the INE eCDFP exam - a student who wants the certificate sits it themselves. ITGate promises the certificate's full content taught to exam-ready depth, plus its own final exam to confirm understanding.** Consequences: (a) coverage of **every INE section stays mandatory** - *same content* is the promise being sold, and `topic_map.md`'s placement obligation is untouched; (b) **`D24`'s domain tolerance relaxes from <= 1.0 pp to <= 2.0 pp and becomes a health check, not a gate**; (c) a deliberate lean toward **Preservation and Reporting** is allowed inside that band. The exam weights remain the design target because a student must still be able to sit INE tomorrow. | Stated 2026-09-08. A <= 1.0 pp tolerance is false precision once no one is actually scored against those weights, and it was the constraint that made Preservation and Reporting - the two things the job and the `D20` rubric actually grade - look like overspend. Keeping the weights as a target rather than discarding them preserves the exam-ready promise; keeping coverage mandatory preserves the *same content* promise. The academy final exam is a new deliverable and needs its own decision row. |
| 2026-09-08 | **D75** | **The academy's final exam is MCQ-only, run by ITGate, and is out of scope for this build. It is not designed, written or stored in this repo, and no part of the course is shaped around it.** Supersedes the closing note of `D74`, which said it was a new deliverable needing its own row - it is not a deliverable of ours at all. **The assessment of record remains the forensic report on the `D20` rubric, every session, plus the per-session quiz.** | Ebrahim, 2026-09-08: the academy exam is multiple-choice and has nothing to do with him or with what is being built here. Recording it explicitly prevents a future session from 'helpfully' designing a final exam, and removes the one thing that could have pulled assessment away from the report - which is the whole point of `D7` and `D20`. |
| 2026-09-08 | **D76** | **The material becomes bilingual in place: simplest-possible English, with a short Egyptian-Arabic explanation line directly under each English block.** Supersedes Part 1's 'Arabic is spoken explanation only'. Seven rules govern it: explanation never literal translation; technical terms stay English inside the Arabic; Egyptian spoken register, not MSA; **the Arabic must be shorter than the English it sits under - if it is not, the English is too complicated and gets simplified**; one Arabic line per block, not per sentence; if it adds nothing it is deleted; visually secondary (smaller, muted, correct RTL, one token pair in `design_system.md`). Applies to the session page, `student_guide`, `guided_lab`, and quiz/homework **stems**; the report the student writes stays English; instructor-internal documents are English. | Ebrahim, 2026-09-08, with a layout reference image: not every student reads English well, but the certificate content and the professional deliverable are both English, so removing English is not an option and neither is leaving weaker readers behind. Putting the explanation **in place** rather than in a separate translated document is what makes it get read. Rule 4 is the one that earns its keep - it turns the Arabic layer into a permanent readability check on the English half. **Cost, accepted knowingly:** page height grows, the density gate needs re-baselining, and every page and package already built (S1-S3) must be retrofitted. |
| 2026-09-08 | **D77** | **`instructor_script_ar.md` is promoted to a required document - #10 in every session package - with a fixed six-section structure**: pre-class checklist · session map on the clock (`D71`) · **page by page** (what you say, how it links to the previous page, how it links to the lab, the terms on that page and how to answer them) · **lab step by step** (what you type, expected output, what to say while it runs, what goes wrong) · **from the practical to the report** (how what was just done becomes report lines) · **the questions students actually ask**. Egyptian Arabic, terms in English, short and direct. It tells the instructor what to **do**, never what a topic **is**. | Ebrahim asked for it explicitly on 2026-09-08. It already existed for S1 (474 lines) and S2 (379) but was never a required document, so S3 shipped without one - the exact drift a fixed package list prevents. Part 8's list moves from 9 documents to 10. The two existing scripts state the pre-`D71` clock (130/60/15) and must be updated to 70/15/60/60/15. |
| 2026-09-08 | **D78** | **`D76`'s cost is absorbed by measurement and type, never by cutting content.** (a) **`density_gate.py` counts English text only** - the Arabic layer is excluded by its class, and the existing limits (4000 total / 180 average / 250 worst page) are unchanged. (b) **Two new gate checks** replace what that exclusion gives up: every teaching block carries its Arabic line, and **no Arabic line is longer than the English above it** - `D76` rule 4 becomes machine-checked rather than trusted. (c) **The type scale drops one step**: English body down ~6 %, the Arabic line at ~`0.86em` of its English, muted, with a hard **floor of 12 px rendered at any breakpoint**. (d) **Longer pages and more pages per session are accepted and are not a defect.** | Ebrahim, 2026-09-08: material size is not a problem, and the type may go slightly smaller so it looks right. The gate exists to stop walls of prose, and the Arabic layer is not prose - it is the same prose again, for a reader who needs it. Counting it would have forced exactly the outcome the gate was built to prevent: cutting real teaching to make room for a required feature. Machine-checking rule 4 matters more than it looks - it is the rule that keeps the English simple, and a rule nobody measures is a rule that decays by S4. The 12 px floor is there because 'slightly smaller' has no natural stopping point. |
| 2026-09-08 | **D79** | **The course is organised by TOPIC, not by session. The topic is the unit of material; a session is only a container that holds as many topics as fit the clock on the day.** Every topic is a complete self-contained unit in four parts: **bridge + theory** (opens with the link to the topic before it) - **guided practice** - **independent work on the carry-through case** - **the report stage it contributes, written now**. Theory is <= 35 % of a topic's minutes. The session container keeps **205 + 15 break = 220**, with the break **at a topic boundary inside the first two hours, never mid-topic**, and the final 15 min staying a session-level integrity close. **Supersedes `D71`'s A/B1/B2/C session layout** while keeping its proportions, its report-during-practice rule, its keyboard rule and its break rules. **Topic IDs become `T01`..`Tnn`, sequential in teaching order and session-independent; `SN-NN` is retired.** | Ebrahim, 2026-09-08, choosing option A: each topic taught complete before the next begins, with a link between topics opening every theory part. This dissolves the problem that forced the question - `S6` did not fit `D71` because it carried three subjects, and under a topic-based map no session owns its content, so no session can be overloaded and the 6-vs-7 session question no longer has to be answered in advance. It also matches the subject: DF is a sequence of stages, so a stage is the natural unit. Session-prefixed IDs had to go with it - an ID naming a session is a lie in a course whose sessions are decided on the day. |
| 2026-09-08 | **D80** | **Topic ordering is machine-verified, and the verification is a gate.** Every topic declares the topics whose result it uses; the chain is topologically sorted and checked for: no topic before a prerequisite, no cycles, no prerequisite that does not exist, every topic reachable. **Evidence counts as a prerequisite** - a topic cannot appear before the evidence it needs has been acquired. A failing order does not ship. | Ebrahim's own emphasis, 2026-09-08: the order must be right, and a topic out of place leaves a gap the student never recovers from. Under `D79` this stops being a preference and becomes the load-bearing structure - the session no longer protects the sequence, so the sequence has to protect itself. `topic_map.md` already carries a Prerequisites column that was decorative; this makes it the thing the map is built from. Cheap to check, and the one error class that is invisible until it is being taught in a room. |
| 2026-09-08 | **D81** | **The course goes beyond the eCDFP certificate: Linux forensics and mobile forensics are added as extension topics. Reverses `D38` (Linux out of scope) and the earlier exclusion of mobile.** They are **additions, never replacements** - every INE section stays covered, so `D74`'s *same content* promise holds. They sit **outside the `D24` domain reconciliation**, which is computed on INE-mapped topics only. They are labelled **beyond certificate** wherever they appear. **Evidence:** Linux is Tier 1 (a Linux victim VM joins the lab as a fourth machine); **mobile cannot be Tier 1** - `R9` forbids real personal data and a real handset cannot be imaged for teaching - so mobile is **Tier 2 only**, a purpose-built public research image, linked never rehosted, and **if none can be licensed the topic is cut rather than faked**. The course lands at **7 x 4 h = 28 h**, inside `D70`'s ceiling. | Ebrahim, 2026-09-08: he wants students to finish as complete DF practitioners, not certificate-shaped ones, and real casework is not Windows-only - servers and phones carry evidence too. Keeping the additions outside the domain maths is what makes this safe: the exam-readiness promise of `D74` and the completeness goal stop competing, because each is measured against its own baseline. The mobile evidence rule is stated up front because it is the one topic here that can quietly become fabricated evidence, and `R9` does not bend for convenience. |
| 2026-09-08 | **D82** | **The material carries two tiers: TAUGHT and SELF-STUDY. `D81`'s Linux and mobile topics - and any further completeness topics - are SELF-STUDY: they live on the site, carry no minute budget, take no session time, get no instructor script, and are excluded from the `D24` reconciliation.** Corrects `D81`'s '7 x 4 h = 28 h' line: **the taught course stays at 1140 topic minutes / 190 = exactly 6 sessions.** Extension content never grows the taught course. Self-study evidence is **Tier 2 only** - linked public corpora, never rehosted. **The governing rule: a self-study topic ships complete or it does not ship** - same `R10` six-box standard, same findings-vs-interpretation discipline, real licensed evidence, and an answer key for anything it sets. No stubs, no 'coming soon' tiles. | Ebrahim, 2026-09-08: he will not teach Linux or mobile in class - he is adding them so a student who wants to become a real DF practitioner has them, and it has nothing to do with the lecture. This resolves the tension `D81` created between completeness and the 24-hour slot: they were only competing because both were being paid for in class minutes. The ship-complete rule is the whole risk of this tier - the `D59` review measured six announced-and-never-built labs in the previous material, and an untaught tier is precisely where that failure returns unnoticed, because nobody is standing in a room discovering it is empty. |
| 2026-09-08 | **D83** | **The self-study tier is a physically separate section of the material and the site, reached after the taught course and never mixed into it.** Taught: `design/topic_map.md` (**INE eCDFP topics only**), `packages/topic-NN/`, `docs/topic-NN/`. Self-study: `design/beyond_map.md`, `packages/beyond/mNN-<slug>/`, `docs/beyond/` with its own index. **`topic_map.md` never carries a self-study row**; no session page links into `beyond/`; no `beyond/` module is a prerequisite of anything taught, so the `D80` dependency chain covers the taught tier only. **All twelve catalogue modules are in scope**: Linux (x2), Mobile (x2), macOS, VM forensics, Cloud, Database, Encryption, Anti-forensics detection, Advanced memory, Enterprise DFIR at scale, Legal & expert testimony, Malware triage. | Ebrahim, 2026-09-08: add them all, but keep them in a separate part so students can study them properly after finishing, and keep our own material organised by the INE eCDFP topics. Physical separation is what makes `D82`'s promise enforceable rather than aspirational - once a self-study row can sit in `topic_map.md`, the taught course starts absorbing untaught content by accident, and the `D24` reconciliation and the 'same content' promise of `D74` both quietly stop meaning anything. Keeping the dependency chain taught-only also stops a student being blocked on a module nobody teaches. |
| 2026-09-08 | **D84** | **The taught map is locked: 25 topics on 14 pages, 1140 topic minutes.** A page holds one or two topics (`P13` holds three); **each topic keeps its own bridge, theory, guided practice, independent work and report stage even when it shares a page** (`D79`). Topics share a page only where they are the same stage of the work - never to fill space, which is why `T22` Internet & Email joined the network page and `T23` Volatility merged with memory acquisition rather than sitting as 15- and 20-minute orphans. **1140 / 190 = exactly 6 sessions, so `D1` holds without using `D70`'s valve.** Average page 81 min, about 2-3 pages a session, chosen on the day. The six 15-min hash-verify/CoC rows leave the topic map entirely - they are the session integrity close (90 min total, budgeted per session). The self-study catalogue is **14 units, `M01`-`M14`**, with no minutes. The pre-`D79` session roadmap table is removed from Part 4. | Ebrahim approved locking the map on 2026-09-08 after reviewing the 63 rows grouped and rejecting any merge of unrelated topics. Locking it now is what unblocks everything else - the report staging, the per-topic document set, the page shape, the evidence sets and the site all key off it. The two merges he forced (`T22`, `T23`) turned out to improve the sequence rather than compromise it: memory is now acquired and read in one place, which is how memory forensics is actually practised, and the existing declared prerequisite (`S6-11` needs only `S2-02`) already permitted it. |
| 2026-09-08 | **D85** | **The student writes ONE cumulative report for the whole course, on the carry-through case, growing as topics are learned - not one report per session. Twelve professional sections; `D20`'s four criteria grade the growing document at every session.** (1) **Sections fill in learning order, not report order** - writing section 8 before section 3 is correct and is said to students out loud. (2) **A complete worked model report ships on the report page from day one**, written to professional standard, **on a DIFFERENT case from the student's** - a model on the same case is an answer key. (3) **Every topic's report stage names the exact section it fills and shows a good entry for it**; a topic that does not name its section is unfinished. (4) **`T25` is the assembly, taught as its own skill** - reorder, write executive summary / interpretation / limitations / conclusions last, verify no section is empty. A **section tracker** on the report page shows filled vs empty, which is needed precisely because filling is out of order. | Ebrahim, 2026-09-08: one report like a real DF report, each newly learned piece goes into its part, the order does not have to be tidy, and what matters is that the student knows how a real report is divided and has a real written example in front of him. One growing report is also the only shape consistent with `D19`'s single carry-through incident - six separate reports on one case is not something an examiner would ever produce. The different-case rule on the model is the load-bearing detail: a fully worked model of the case the student is investigating hands them every finding. |
| 2026-09-08 | **D86** | **The instructor package is one folder per PAGE - `packages/page-NN/`, fourteen folders - and site pages follow the same unit, `docs/page-NN/`. Inside every document each topic keeps its own clearly marked section, so sharing a folder never merges two topics.** **`session_plan.md` is removed**: sessions are composed on the day (`D79`), so a per-session plan cannot be written in advance; it is replaced by **one** `design/delivery_planner.md` listing every topic with minutes, page, prerequisites and evidence, which the instructor reads to build a day. The list becomes **11 required documents**: instructor guide · student guide · guided lab · student activity · quiz + key · homework + key · **`report_stage.md` (new, `D85` rule 3)** · Arabic delivery script · build log. | Ebrahim chose page-level folders on 2026-09-08 after the trade-off was laid out: 154 files instead of 275, and one folder open on the day instead of four. The page is the build and delivery unit even though the topic is the pedagogical unit, and `D79`'s separation survives inside the documents rather than being enforced by the filesystem. Removing `session_plan.md` was forced, not chosen - six plans for sessions whose contents are decided that morning would have been the first documents to go stale, and the previous material's worst defect was exactly that: plans describing labs that were never delivered. |
| 2026-09-08 | **D87** | **Figure `F7` is formalised: the carry-through incident is six numbered stages across two named hosts, and every stage is bound to the topics that prove it.** Hosts: **`FIN-WKS-07`** (Windows victim, Meridian Retail Group, user `l.bennett`, = exhibit `EVI-SRC01`) and the **Kali host** as the receiving end - **deliberately NOT examined, stated as a limitation**, since a 3-VM lab has no domain controller and the lateral leg uses local harvested credentials. Stages: (1) **phishing email into l.bennett's mailbox, macro attachment opened** - refined by `D73`, replacing the earlier undefined delivery; (2) execution + persistence; (3) **HTTP** C2 beacon to a documentation IP; (4) lateral movement to the Kali host; (5) collection and the `customer_export` archive staged then deleted; (6) exfiltration to USB, ransomware tooling staged but never fired, then anti-forensics (log clear + one timestomp). Attack window **24-26 Aug 2026**, discovered ~1 week later. Defender real-time protection disabled before staging, recorded as a limitation. **A stage-to-topic table now lives in `staging_plan.md` and every stage has a topic that reaches it.** | `F7` was locked in `staging_plan.md` on 2026-09-06 with an explicit TODO to add a decision row, and the row was never written - so the one figure every session page depends on existed only in a gitignored instructor file. Formalising it also surfaced the collision with `D73`: `F7` never said how the document arrived, and `D73` requires it to arrive by mail so `T23` can recover it. Binding stages to topics is the check that matters - it is how you find out, at design time rather than in a classroom, that a stage was staged into the evidence and then never taught. |
| 2026-09-08 | **D88** | **One page shape, composed from its topics - replaces the Tier A / Tier B session shapes.** Cover, how-it-works, recap-bridge; then for **each** topic: bridge, theory (artifacts on `R10`'s six boxes, `SOC` chip where real), guided lab, independent work, report stage, knowledge check; then cheat sheet + *what you can now sign*, homework, references. **No break page** - the break falls at a topic boundary chosen on the day (`D79`), so the 15-min timer moves into the page furniture. **The Tier B catalogue survives as a within-topic layout** for artifact-dense topics, not as a second page type. **At least one 'this artifact cannot prove that' question per page** - stricter than `D60`'s per-session rule. | The old shapes were session-shaped and prescribed a page count (~22 pages, or 8-15 micro-pages); under `D79` a page is its topics and the count falls out of them. Keeping Tier B as a layout rather than a page type preserves the one thing it was good at - many small uniform artifacts - without reintroducing a second global skeleton that would drift from the first. The break had to leave the page: a fixed break page in a course whose sessions are composed on the day would be wrong on most pages most of the time. |
| 2026-09-08 | **D89** | **Pre-work is only ever recall from an earlier course in the track, evidence download + hash verification, or tool installation - and it is never new examinable material.** If a page's pre-work would teach something new, that content moves into the page. Each item states its time and a one-line 'how you know you did it' check. **The depth rule:** breadth is fixed by `D74` (every INE section covered); depth is allocated by three tests - does it appear in the carry-through case (`D87`), does it produce a line in the report (`D85`), is it a known misread? Three of three earns full depth; zero of three earns a mention and a reference. **Cut first:** history and background, vendor comparisons that change no decision, anything with no artifact behind it. **Never cut:** `R10`'s *does NOT prove* box, the report stage, the answer key, the bridge. | Depth was being decided ad hoc, which is how `D58` came to drop INE's largest single section (69 pages of executable analysis) while smaller material survived - a cut nobody defended because nobody had written down what makes a topic worth minutes. The three tests are not arbitrary: they are the course's own promises (the case, the report, the misreads it exists to prevent), so a topic that fails all three genuinely is not what this course is for. Naming what is never cut matters more than naming what is - under time pressure the *does NOT prove* box is exactly what gets dropped, and it is the whole subject. |
| 2026-09-08 | **D90** | **The site follows the `D84` map: `docs/page-01..14/` replaces `docs/session-01..06/`; the six `brief.html` files are removed (a per-session brief cannot exist for a session composed on the day); the six `record.html` files collapse into one cumulative `docs/record.html`; `docs/report/` is new (the twelve sections, the worked model report on a different case, and the section tracker, `D85`); `docs/beyond/` is new (`D83`); `roadmap.html` is regenerated as 14 pages / 25 topics / the dependency chain rather than six sessions.** Nothing is deleted until its replacement exists and passes the Part 9 gate; superseded files move to `_to_delete/` because the mount cannot delete (`R3`). | The site was built as a session platform in August and `D79` made that shape wrong. Collapsing the six `record.html` copies is the clearest case: the chain-of-custody record was **always** cumulative (`D55`) - having six of it was a concession to the session layout, and with that layout gone the duplication has no defence left. |
| 2026-09-08 | **D91** | **The evidence gate (Part 8 step 0) moves from the session to the page: a page is not built until every evidence set its topics name is verified in `evidence_sets.md`.** `ecdfp-evidence` still owns the IDs and remains the only thing that may mark a set verified. **The six cases keep their numbers and bind to topics:** Case 01 -> `T03`, Case 02 -> `T09`, Case 03 -> `T10`, Case 04 -> `T13`, Case 05 -> `T20`, Case 06 released at the end of the fifth **session** and debriefed in `T25`. | Under `D79` the session no longer owns content, so a session-level evidence gate has nothing to gate. Per page it becomes sharper, not looser - the unit being blocked is the unit being built. Case 06 is the one thing that legitimately stays session-bound: it is a release event in the calendar, not a piece of material, so it is carried in `delivery_planner.md` where the instructor composes the day rather than in the topic map. |
| 2026-09-08 | **D92** | **Typography and layout are set by one fact: the page is read on a projector by someone not sitting at it.** (1) **`D78`'s one-step reduction of the English type is cancelled** - the bilingual height is absorbed by longer pages, which is already accepted; only the Arabic line is smaller. (2) The topic title is visibly larger than its body. (3) **No stacked sentences: a prose block is never narrower than 45 characters, target measure 65-80**; anything narrower carries labels only. (4) **The middle column is for the topic and its explanation - notes, caveats, asides and `SOC` chips live in a side rail** and never interrupt it. (5) **Blocks are placed on a grid, left or right, not centred by default.** (6) Each part of a topic carries a visible position marker and states its transition, so the reader feels the move from one point to the next. **The Arabic sits in the same box as its English, after it, with a clear gap above it, `dir="rtl"` and right-aligned** - never a separate box or a parallel column. | Ebrahim, 2026-09-08. Cancelling `D78`'s type reduction is the important reversal: it was a reasonable trade when page height looked expensive, but he then said length is not a problem and legibility is, so the trade no longer buys anything worth its cost. The 45ch floor is the concrete form of his complaint about sentences broken into towers - a rule a checker can enforce, unlike 'make it look right'. Moving asides to a side rail is what protects the main column: a caveat dropped mid-flow reads as part of the argument and breaks it. |
| 2026-09-08 | **D93** | **The material uses real images, and this is a requirement.** Our own lab capture is preferred **wherever equally good** - not for licensing but because it shows the student their own case. `Resources/Instructor` is used freely: it is the previous ITGate instructor's own work, not third-party courseware. **THM, INE, vendor and web images are used for teaching, each carrying a visible source line beneath it** (small, muted: *Source: TryHackMe - room* / *Source: INE eCDFP, Module N*). **Tool logos are the official logo, never a substitute.** Every image is registered in `design/image_sources.md` (file, what it shows, source, URL/document, date) - **an image with no row is not published**. `R9` overrides everything here: never real personal data, real credentials, a real case or a real person's device. | Ebrahim, 2026-09-08. I raised the public-repo copyright exposure and he decided: he is teaching, not republishing as his own, the labs are publicly solvable, and attribution goes under each image. He also corrected a real error of mine - `Resources/Instructor` is the academy's own instructor material and never belonged in that concern at all. The attribution register is the part that makes his mitigation real rather than stated: a source line that exists only as an intention is the first thing that goes missing at page forty. Our-own-capture stays the preference on teaching grounds, since a screenshot of INE's evidence shows a stranger's case and a screenshot of our lab shows the one the student is investigating. |
| 2026-09-08 | **D94** | **The diagram system is defined by ten patterns, extracted from four reference animations Ebrahim supplied.** The governing one: **the whole system is on screen from frame one - the structure never appears or disappears, only its state changes.** The others: parallel rows with identical layout so only the difference moves · a spotlight walk that lights one row and fades a plain-language line into it · a step counter or status banner · **accumulation** (chips stacking to show structure being built) · live values changing in place · real window chrome · a changing caption stating the takeaway · one colour per concept held course-wide · dark ground with zero decorative motion. **Nearly everything gets a visual** - prose-only is the exception and must earn it. **`D51` is extended to two animation modes: looping with no button** (comparisons and cycles, respecting `prefers-reduced-motion`) **and click-to-play** (mechanisms with a story), whose SMIL rules are unchanged. **No fabricated tool output, ever** - terminal cards, hex panes and tool windows carry real captured output. | The four references (OSI encapsulation, HTTP methods, load-balancing algorithms, REST vs queue) all use the same formula, and it is a better formula than ours: we animate a story that plays once, they hold a whole system still and move a spotlight through it, which is what makes a comparison teachable. Pattern 2 alone answers several of this course's hardest pages - Prefetch vs Amcache vs ShimCache is the misread `S5` exists to prevent, and it is exactly a parallel-rows figure. Pattern 5 turns out to describe `D85`'s growing report. The no-fabrication rule is not new (`R9`, and the repeatedly-recurring fabricated-artifact defect) but it is now also the reason the pages will stop looking like cartoons - which is what he actually complained about. |
| 2026-09-08 | **D95** | **`SIMSCREEN` is the named standard visual for theory: a procedure shown as a screen being operated, with every screen built from scratch in HTML/CSS - never a screenshot with overlays.** Real screenshots move to the **practical** layer, captured by the instructor on `FOR-WS01` against our own evidence. Fixed anatomy: one step = one action on one screen; animated cursor to the target, pulsing ring, spotlight dimming the rest; bilingual caption per step; step counter, dots, PREV/PAUSE/NEXT; auto-plays and loops so the instructor can pause and talk over it; **exactly one step may be marked `key`** (amber - the decision the procedure turns on, and if no step qualifies the procedure does not need a `SIMSCREEN`); hotspots resolved from the DOM at runtime so they cannot drift. **Invoked as "simscreen for <topic>".** It also **refines `D94`'s fabrication rule rather than breaking it**: a recreated *interface* is a diagram of a UI and is allowed; a *finding* - hash, artifact value, path, timestamp, output presented as something our case shows - must be real, and carries a visible provisional line until the lab produces it. | Ebrahim, 2026-09-08, after seeing a screenshot-plus-overlay prototype: he wants the theory layer to be a recreated walkthrough of what is about to happen, and to keep real captures for the practical. That split is better than either alone - the theory screen can be perfectly clean, correctly sized, bilingual and infinitely sharp because it is DOM rather than a 438px JPEG, while the practical keeps the honesty of a real capture of the student's own case. It also removes the third-party image question from the theory layer entirely. Naming it was his request and it earns its keep: the component is now a thing he can ask for by name on any topic instead of re-describing it. |
| 2026-09-08 | **D96** | **`SIMSCREEN` becomes shipped infrastructure, not a per-page build**: `docs/assets/js/simscreen.js` (the engine), `docs/assets/css/simscreen.css` (the shell) and **`docs/assets/css/win-ui.css` - a recreated-interface library** (title bar, menu bar, dropdown menu, fieldset+legend, radio, checkbox, text field, list box, default/disabled buttons, combo, progress bar, status bar, Start menu, Explorer pane, terminal, hex pane), each matched to real Windows metrics. A page supplies only its screens and its steps array. **A `SIMSCREEN`'s steps ARE the steps of `guided_lab.md`** - one list, so the theory walkthrough and the practical lab cannot drift. **Every procedure a student will perform gets one**, named at the outline gate (`D9`) and recorded in `build_log.md`. **A ten-check quality bar** gates each one: DOM only / correct Windows metrics / one action per step / exactly one `key` step / captions give the reason / Arabic shorter than English / DOM-resolved hotspots / play-pause-nav-reduced-motion verified / findings carry the provisional line / steps match `guided_lab.md` one for one. | Ebrahim approved the prototype on 2026-09-08 and said he will need many of them at that quality or better. Quality at volume is not a matter of effort - the twelfth one is built at 2 a.m. and the difference shows - so it has to come from a kit and a checklist rather than from care. Binding the steps to `guided_lab.md` is the part that pays off later: the commonest rot in course material is a walkthrough that still shows the old procedure, and here that failure is structurally impossible. |
| 2026-09-08 | **D97** | **The `D80` dependency gate caught two real ordering errors in `D84`'s grouping on its first run, and both are corrected in the generated map.** (1) **Case 01 (`S1-10`) moves from `T03` to `T04`** - it declares `S1-09` (hex and magic bytes) as a prerequisite, so grouping it with hashing and chain of custody put the investigation before the skill it needs. It is `T04`'s independent work, which is what `D79` part 3 is for. (2) **`S2-06` file-signature-vs-extension moves from `T04` to `T09`** - its own row says *run it against the image you just made*, so it cannot sit two pages before the image exists. Resulting sizes: `T03` 38 min, `T04` 82, `T09` 112; pages now run 38-145 min, average 81. Course totals unchanged at 25 topics / 14 pages / 1140 min, and the domain reconciliation improves to a maximum 0.6 pp deviation. | The gate paid for itself on its first execution, which is the argument for `D80` in one line. Both errors were invisible in the page-grouping table Ebrahim reviewed - they only exist in the prerequisite edges, and both would have surfaced in a classroom as a student being asked to find a tampered file before being taught to read one, or to test file signatures against an image that had not been made yet. The fix also improved the design rather than merely satisfying the checker: Case 01 as `T04`'s independent work is exactly the shape `D79` describes. |
| 2026-09-08 | **D98** | **The diploma is 7 x 4 h = 28 hours, not 6 x 4 h. `D70`'s valve is invoked for the first time, and this is the case it was written for.** `1140 / 190 = 6.0` is only achievable with perfect packing; topics are indivisible and their order is fixed by `D80`, so an optimal balanced split lands on **seven days, largest 190 min, all inside the 210-min ceiling**. Also settled by the ordering gate's first full run: **`T08` (imaging scope, formats, HPA/DCO) declares no evidence set** - it is taught on the **source device**, since `EVS-02` does not exist until `T09`'s guided lab creates it; and **the five Tier 3 synthesized sets (`EVS-01`, `05`, `07`, `08`, `10`) are available from day one**, so they are never an ordering constraint. `scripts/order_gate.py` is now a permanent gate with nine checks and it passes. | Arithmetic, not preference: an average of exactly 190 leaves no room for a 112-minute topic in a fixed sequence. Seven is honest and cheap - it is inside `D70`'s ceiling of eight, no content is cut, and the alternative was splitting coherent topics purely to satisfy a divisor. The `T08` correction is the more interesting one: the old map tagged it `EVS-02` because the session containing it eventually produced that image, which a session-shaped map could hide and a topic-shaped one cannot. |
| 2026-09-08 | **D99** | **The forensic report template goes from ten sections to the twelve of `D85`, and the `D20` rubric is re-pointed in the same change.** Three sections are genuinely new: **§2 Executive summary** (written last, read first), **§4 Chain of custody as its own section** (it was a table inside the old §10 Exhibits), and **§7 Acquisition details** (it was folded into Method). Old §4 Tools and §5 Method merge into **§6 Tools and method**. The rubric's **wording is unchanged** - only the section numbers it cites: Integrity §4·§5 (was §3·§10), Method §6·§7 (was §4·§5), Findings §8 (was §6), Separation §9·§10 (was §7·§9). The canonical source moves to **`design/report_template.md`**; `packages/*/report_template.md` is retired (`D86` gave each page `report_stage.md` instead) and the student-facing copy is `docs/report/`. | Adopted 2026-09-08 at the `P01` outline gate, where the collision surfaced: `T02` teaches the template, and the template in the repo did not match the one `D85` had just specified. Re-pointing the rubric in the same change is the part that matters - criterion 1 cited §3·§10, and under twelve sections §3 is now Authorisation and §10 is Limitations, so leaving it would have had the integrity criterion grading the wrong two sections. `D20`'s 'the rubric never changes' promise survives because what it measures is identical; only the pointers moved. The three additions are what a professional report has and the old template lacked, and §7 earns its place on its own argument: acquisition is the one step that cannot be repeated, so it is not a paragraph inside Method. |
| 2026-09-08 | **D100** | **A third report template was found and retired.** `docs/resources/report.html` published a **7-section** structure (Summary · Evidence handled · Method · Findings · Interpretation · Limitations · Chain of custody) - different from both the 10-section `packages/session-01/report_template.md` and `D85`'s twelve. **The project was teaching three different report structures.** All three are now one: `design/report_template.md` is canonical, `docs/report/` is generated from it, and `docs/resources/report.html` is deleted with every link repointed. Its two genuinely good blocks - the worked criterion-4 triple on `USBSTOR`, and the secrets-and-personal-data rule - were carried over verbatim. | Found at the `P01` build, because `T02` teaches the template and the page it linked to disagreed with what was being taught. Three templates is worse than either one of them: a student who reads the site, the package and the instructor's slides gets three answers to *what is a forensic report*, and the rubric can only point at one. Generating the page from the canonical file is what stops a fourth appearing - the drift happened because the page was hand-written and nothing tied it to anything. |
| 2026-09-08 | **D101** | **A page is a DECK OF SCREENS, one idea per screen — not one long scroll.** `.page-layout` -> `.sidebar` + `.main` with `.page` sections + `.page-nav`, driven by the existing `docs/assets/js/session.js`: prev/next, arrow and Home/End keys, `#pN` deep links, live counter. Only one screen is visible at a time; ~20 screens per page is normal. **The rule that decides where a screen ends is *one idea*** - if a screen carries two things a student could be asked about separately, it is two screens, and a screen that scrolls has already failed. `P01` was rebuilt from a single 504-line scroll into **21 screens**. | My error, caught by Ebrahim on the first built page: *'everything on one page, there is no focus.'* He had already approved the navigation as it stood - the paginated deck existed, was verified, and `session.js` drove it - and `D88` even listed prev/next nav among the things every page keeps. I wrote the skeleton as a flowing list anyway and built a scroll. Recording it because the failure is easy to repeat: `D88`'s section list reads as a document outline, and nothing in it said *these are screens*. Now it does. |
| 2026-09-08 | **D102** | **The Arabic line sits directly under the English it explains, and EVERY explanatory element carries one - not just paragraphs.** Two fixes. (a) **Placement:** `.ar` is `width:fit-content` with **physical** `margin-left:0; margin-right:auto`, so its box hugs the start edge instead of being thrown to the right of a 1136 px column. `margin-inline-end` was the bug - it resolves against the **element's own** direction, and `.ar` is `rtl`, so 'inline-end' meant LEFT and pushed the box the wrong way. Measured: every Arabic block now starts within 0 px of its English. (b) **Coverage:** table cells, list items, box bodies and worked examples all carry Arabic - `P01` went from 65 to **122** Arabic blocks. Every finding/interpretation/limitation box also gains a **worked example** (`.eg`), with its own Arabic. | Ebrahim, on the first built page: the Arabic was 'too far from the sentence', then twice more - 'where is the Arabic under each one', 'I want every single thing that is explained to have its part in Arabic underneath'. The placement bug is worth recording because it is invisible in code review and only shows in a wide column: the rule read correctly and did the opposite. The coverage rule is the bigger change - `D76` said 'one Arabic line per English block' and I read 'block' as 'paragraph'. A table row **is** a block, and a student who cannot read the English cell cannot read the table. |
| 2026-09-08 | **D103** | **`simscreen.js` re-places its cursor, ring and spotlight whenever the figure becomes visible, and on hash change and resize.** A `SIMSCREEN` inside a hidden `.page` measures 0x0 (`D101` - a page is a deck and only one screen is displayed), so the hotspot computed at init was junk: the ring and cursor pinned to the corner while the spotlight looked right. `place()` now returns early on a zero-size holder rather than writing garbage, and an IntersectionObserver, a `hashchange` listener and a ResizeObserver each trigger a re-place. | Found by rendering `P01#p7` in Playwright rather than trusting the structural check, which passed while the figure was visibly wrong. It is the direct interaction of two decisions made hours apart - `D96` resolves hotspots from the DOM (right), `D101` hides all but one screen (right), and together they break unless something re-measures. Every SIMSCREEN in the course would have shipped with its pointer in the corner. |
| 2026-09-08 | **D104** | **The Arabic layer is rewritten as EXPLANATION in professional written Arabic, and `D76` rules 1 and 3 are replaced.** (a) **Never mirror the English sentence.** The Arabic answers *what does this mean and why does it matter*; the test is to cover the English and ask whether the Arabic teaches the idea alone. (b) **Modern Standard Arabic, textbook register** - heavy Egyptian colloquial (`اللي` `مش` `عشان` `دلوقتي` `كده` `بتاع` `يبقى`) is out of written material; spoken Egyptian stays in `instructor_script_ar.md`, which is the instructor's own voice. (c) The length rule relaxes from *shorter than the English* to *about the same* - an explanation may need a clause a translation would not. All 122 Arabic blocks on `P01` were rewritten. **Also in this pass:** the `§` glyph is removed course-wide in favour of the word *Section*; the reading measure widens 78ch -> 92ch so an Arabic line stops wrapping to two; comparison figures slow from 4.5 s to **7 s per row** and SIMSCREEN from 4.8 s to **7.5 s per step**; and the terminal is recoloured onto the site palette instead of the Windows console's. | Ebrahim, reading the first built page: *'the literal translation is genuinely very bad'*, *'I do not understand what the Arabic means - is it explaining the term or summarising?'*, *'speak more in Arabic than Egyptian so it stays professional'*. He is right and the failure was mine: `D76` rule 1 said 'explanation, never literal translation' and I wrote translations anyway, because writing a mirror is easier than deciding what the sentence actually teaches. Rule 3 made it worse - I read 'Egyptian spoken register' as licence for colloquial written material, when what he wanted colloquial was the instructor's script. The cover-the-English test is now in the rules because it is the only check that catches this. |
| 2026-09-08 | **D105** | **A first table column keeps its LABEL unbreakable, never the whole cell — and `.ar` always wraps.** `.page table td:first-child:not(:has(a)){ text-wrap:nowrap }` put the English **and its Arabic explanation** on one unbreakable line, which set a huge max-content width and starved every other column: on `P01#p10` the two-column table resolved to **849 px / 179 px** at a 1020 px measure, so the right-hand cell wrapped its Arabic to **seven lines**. Replaced by: the cell wraps like prose; `> b` and `> code` (the label markers) stay `nowrap` above 700 px; `.ar` is `white-space:normal` everywhere, so it can never inherit a label's `nowrap`. The same table now resolves to **500 px / 520 px**. | Ebrahim: *'there is space left and right — widen the box so it is one line, not two, and on every page.'* I had read that as a measure problem and widened the prose measure, which did nothing, because the cause was a `nowrap` written for short label columns and applied to every first cell in the course. Worth recording because the symptom appears in the **second** column while the bug is in the first, and because the fix is content-driven — mark the label, not the cell — so it survives a table whose first column is prose. |
| 2026-09-08 | **D106** | **`.ar` is a FULL-WIDTH right-aligned block; its start edge is fixed, not shrink-wrapped.** `D102` made `.ar` `width:fit-content` hugging the start edge. That pinned the *end* of the Arabic to the English's left margin and let its **start** — the right edge, where an Arabic reader begins — land at a different x on every block: 1094 px in one paragraph, 799 px in the cell below it. `.ar` is now `width:auto` with `text-align:right`, so every Arabic block begins at the same right boundary as the English column, and the cyan hairline marker becomes **one continuous rule down the right of the page**. That rule, not the indent, is what tells the reader *this line is the Arabic*. | Found by measuring the rendered page rather than reading the rule: `D102` fixed a real bug (the box was thrown right) but its cure introduced jitter, which is harder to see in a screenshot and worse to read. The principle it settles: in a bilingual block the two languages share a **measure**, not an indent — English anchored left, Arabic anchored right, both inside the same column. |
| 2026-09-08 | **D107** | **SIMSCREEN scales with `zoom`, and `place()` divides by the zoom it finds.** Two bugs, one cause. (a) The narrow-screen rule was `transform:scale(.82)`, which shrinks the pixels but leaves the old box — so the window still overflowed `.ss-stage` at 480 px and left dead space under itself. It is now `zoom:.74` at 700 px and `zoom:.58` at 520 px, with `.ss-stage{overflow:hidden}` as the backstop. (b) `zoom` broke the hotspot maths: `getBoundingClientRect()` returns **zoomed** pixels but `style.left` on a child of the zoomed holder is read in **unzoomed** ones, so the delta was scaled twice and the spotlight landed at *z × z* of the target — on `P01#p7` it pointed at *NetworkMiner* instead of the selected *TOOLS.sha256*. `place()` now recovers the factor as `rect.width / offsetWidth` and divides. Also: `.ss-mask` drops its `inset:-30px` bleed for `inset:0`, and the matching `+30` offsets come out of the gradient — they had been moving the spotlight 30 px off target at **every** width. | The `+30` pair is the lesson: a magic number written to compensate for a CSS value in a different file, with nothing tying the two together. Changing the CSS silently broke the JS, and the only reason it was caught is that the mobile render put the pointer on visibly the wrong row. Now asserted — the render gate clicks all six steps at 1400 / 900 / 700 / 480 and checks the ring lands on the step's target element: **24/24**. |
| 2026-09-08 | **D108** | **Supersedes `D106`. The Arabic box hugs the START of the English it explains — `width:fit-content`, physical `margin-right:auto` — and the SIMSCREEN caption is uncapped.** `D106` made `.ar` a full-width right-aligned block to pin its start edge. On a wide column that threw the Arabic to the **far right**, level with nothing: a heading read left, its explanation began 900 px away. `D102`'s placement is restored — the Arabic sits **directly under its English line**, both inside one box wide enough for the pair. Separately, `.ss-cap p` loses its `max-width:78ch`: it capped the SIMSCREEN caption at roughly half the frame, left the right side empty and pushed the Arabic onto a third line. Caption now **980 px of 1022**, Arabic **3 lines → 2**. | Ebrahim, seeing `D106` rendered: *'I want it as you had it — the Arabic under the English, in a box that fits both, on the whole page.'* `D106` solved a problem I had measured (the start edge moving from block to block) and not one he had; the jitter costs less than the distance does, because what a reader looks for is **the line above**, not a column edge. Recording it as a reversal rather than editing `D106` because the measurement in `D106` was real — it is the conclusion that was wrong. The `.ar` gate check flips with it: it now asserts the box starts where the English starts, and it counts the parent's **border** as well as its padding, or every `.note` reads as 3 px adrift. |
| 2026-09-08 | **D109** | **`P01` is the reference page. Its look is APPROVED and FROZEN — every page from `P02` inherits it and no page re-decides it.** The frozen contract: a **deck of screens**, one idea each, `session.js` navigation (`D101`) · the bilingual layer as **explanation in professional written Arabic** sitting **directly under its English** in one shared box (`D104` · `D108`) · **FINDING / INTERPRETATION / CANNOT PROVE** encoded three ways — colour, border style, chip (`D27`) · **SIMSCREEN** for procedure, real screenshots for the lab (`D95`) · `D94`'s reveal figures for comparison · **no `§` glyph**, the word *Section* · the report stage on every page (`D85`). A page changes this only by a new decision row here, never by inventing locally. | Ebrahim, on the finished `P01`: *'very good — adopt this look and let us start the material.'* Freezing it is what makes thirteen more pages cheap: the two days `P01` cost were spent on decisions, not on typing, and re-opening them per page would spend them thirteen more times. Recording the contract as a **list of decision IDs** rather than prose so that a page can be checked against it mechanically. |
| 2026-09-08 | **D110** | **`density_gate.py` counts what is ON SCREEN, not what is in the file — and it now enforces the `D78` bilingual checks it was promised.** Four scope corrections, each of which had the gate failing `P01` for a feature the course requires: (a) the **Arabic layer is excluded** from every word count (`D78`) — it restates, it does not add concepts; (b) a **SIMSCREEN is a picture** — its Explorer rows, menu items and terminal output are pixels, and counting them made a lab screen read as a wall of text; (c) a **`.cmp` reveal figure shows one row at a time**, so it counts as its heaviest single row, not the sum; (d) **structural counts come from the raw document**, never from the filtered view. Word totals went 3096 → **2040**, worst page 283 → **170**. Three new checks replace the excluded count: every teaching block carries its Arabic · no Arabic runs beyond **2.2×** its English · **no Egyptian colloquial** in student-facing Arabic (whole-word match plus the `-وش` negation suffix). | The gate had never been run against a bilingual page, and on first contact it produced nine failures of which **five were its own** — a gate that is wrong is worse than no gate, because the honest fix looks like moving the goalposts. Two of the four real findings it did catch are ones nothing else would have: a `<p class="ar">` on `P19` written in **full Egyptian colloquial** that `D104`'s 122-block sweep missed because it matched only `<span class="ar">`, and two blocks on `P02` where the **Arabic was doing the teaching and the English had become a label** — fixed by strengthening the English, not by cutting the Arabic. |
| 2026-09-08 | **D111** | **`docs/index.html` was four documents in one file, and the site pages are now checked structurally like the topic pages.** The retired site generator **appended** its `<main id="pages">` block after `</html>` instead of inserting it before `</body>`, and it had run six times — so the published front page carried **one complete document plus six copies of the pages grid**, each after a closing `</html>`. Browsers render it, which is exactly why nobody saw it: the first document paints correctly and the rest is parsed into the body below the fold. Rebuilt as one document from the newest fragment; `P01` and `P02` are both live cards; the standing line *"None is built yet"* is now *"Two are built"*. | Found while wiring `P02` into the site, from a `grep` that returned three hits for a card that should appear once. The lesson is about **where the gates point**: `render_gate.js` has been run on `page-01` and `page-02` at five widths, and never once on `index.html` or `roadmap.html` — so the most-visited page in the project was the least verified one. A structural check (`<html>` and `<body>` appear exactly once, no `</html>` inside `body.innerHTML`, no page error) now runs on every site page, and it is a one-line assertion that would have caught this the first time the generator misfired. |
| 2026-09-08 | **D112** | **`P02` — Evidence integrity — is built, and it is the first page produced entirely under the frozen contract (`D109`).** 20 screens, 121 Arabic blocks, one `SIMSCREEN`, two `D94` reveal figures, the 11 package documents. **Both gates passed on the first build** — `density_gate.py` ALL PASS, `render_gate.js` zero findings at five widths, and all six `SIMSCREEN` pointers on target at every width. The page turns on one sentence — *a matching hash proves the copy equals the source as read at that moment, and nothing else* — and `FIG P02-F1` is the argument: **four rows whose result line is identical and whose meaning is not** (clean · incomplete via HPA · innocent mismatch after TRIM · a match that proves nothing because one person held both). | The freeze paid for itself immediately: `P01` cost two days of design decisions, `P02` cost none and cleared both gates first time. Two content choices are worth recording. **(a)** The page shows our own courseware stating that hashes prove a file was not tampered with, and correcting itself one slide later — taught as *a respected source was imprecise and you still have to know the truth*, because an analyst who has never seen an authority be wrong learns to trust authorities instead of evidence. **(b)** The independent exercise is a defect hunt in a free room with 18,481 completions that verifies a hash and invalidates it on the next page with a read-write `mount` — and the same exercise requires the instructor to say that the room's audit-trail task is the best treatment of *logging the examiner* in fourteen rooms reviewed. **A source can be worth using and wrong in one place; telling which is which is the skill.** |
| 2026-09-08 | **D113** | **Every page carries real photographs and hand-authored mechanism diagrams. `D94`'s *"nearly everything gets a visual"* is now enforced, and the density gate stops punishing it.** `P01` and `P02` gained **7 inline SVG figures** and **5 real photographs**, in a new `.dgm` component (figure · id · pattern tag · dark ground · bilingual caption) and a `.photo` / `.photo-row` component that carries the licence line as part of the component, not as a style choice. Both pages now put a visual on **16 of their screens**. The five photographs already existed in `docs/session-01/img/`, licensed and hash-verified, and had been **orphaned by the rebuild** — they were moved to `docs/assets/img/photos/` and registered in `design/image_sources.md`, which `D93` requires and which did not exist until now. **And the gate had to change to allow this:** `density_gate.py` counted `<svg>` `<text>` labels as prose, so a well-labelled diagram pushed a screen over the 250-word limit — which pushes the author toward *worse* diagrams. SVG is now excluded from the prose count exactly as `.wu` chrome is. | Ebrahim, on the two finished pages: *"I want more diagrams, more visual content, screenshots and real images. I do not want material that looks like a newspaper."* He is right, and two of the causes were mine. The photographs were sitting in the repo unused because the rebuild moved the pages and nothing moved the images with them — `design/image_sources.md` was specified in `D93` and never actually written, so nothing tied a file to a page. The gate was worse: it was **actively discouraging the thing the design system asks for**, and I would have kept trimming diagram labels to satisfy it. A limit that fights its own design system is a bug in the limit. |
| 2026-09-08 | **D114** | **`figure.photo` is `width:fit-content`, never `max-content`.** With a long caption, `max-content` resolves to the caption's **unwrapped** width — 1306 px at a 480 px viewport — and the `max-width:100%` beside it cannot clamp it, because the parent has no definite width for the percentage to resolve against. `fit-content` caps at the space actually available. | Caught by the render gate the first time a photograph shipped, at 480 px only. Recording it because the rule *looked* right — `width` and `max-width` were both present and both sensible — and the failure needs a narrow viewport plus a long caption together. Same shape as the `margin-inline-end` bug in `D102`: a declaration that reads correctly and does the opposite. |
| 2026-09-09 | **D115** | **Diagrams are STEPPED STATE MACHINES, driven by `docs/assets/js/dgm.js` — not static pictures.** The whole structure is on screen from frame one (`D94` pattern 1) and stepping changes only which parts are lit; the caption under the figure is the takeaway line and **changes with the step** (pattern 8). Controls are deliberately SIMSCREEN's bar — prev · play/pause · next · dots · counter — so the student learns **one** interaction, not two. `data-mode="loop"` auto-plays on scroll into view and pauses when it leaves; `data-reveal="upto"` accumulates, `"only"` spotlights. `prefers-reduced-motion` lights every step at once and hides the controls, so the figure still teaches without moving. **Not SMIL:** `D51`'s SMIL rules exist because SMIL is easy to get wrong — one `<animate>` per element, computed `keyTimes`, no `fill="freeze"` at staggered offsets — and a stepped class plus a CSS transition has none of those traps while giving prev/next/play for free. **12 figures across `P01` and `P02`**, all verified stepping with a changing caption and exactly one control bar. | Ebrahim: *"I want the diagrams animated, not static images — and there are a lot of errors in them, and you put diagrams at the start only; where are they on the rest of the pages?"* All three were fair. **The errors were real:** `P02-D1` drew three inputs converging on one box with a single arrow leaving it toward three unconnected outputs, so the reader could not tell which digest came from which input — now each input has its own lane and the step walks them. `P02-D3` ran its caption under the `verified` pill **and pointed the red arrow into it**, which read as *verification reaches the source* — the exact opposite of the lesson; it is now a barred path with its own band. **The coverage complaint was the sharpest:** figures on the first few screens and none after is precisely the failure pattern 1 exists to prevent, and `P02` gained three more figures for the screens that had none. |
| 2026-09-09 | **D116** | **A build-time width guard runs before any diagram ships, and the homepage duplicate is fixed.** Three `<text>` lines shipped wider than their own `viewBox` in two days, each costing a full render round trip to find. `check_widths()` now estimates every line from measured per-character widths and fails the build first; it caught two overruns the render pass had not yet reached. Separately, `docs/index.html` carried **two** *"The fourteen pages"* sections — the `D111` repair deleted the six appended fragments but kept the one the head already contained **and** the newest one, each with a different card live. One section now, both `P01` and `P02` live. | The guard is the cheap half of a lesson already paid for twice. The homepage is the more interesting one: `D111` fixed a duplication bug **by half** and I verified the result by counting `<html>` tags, which was the symptom I had just been looking at rather than the property that mattered. The check that would have caught it is the one now in Part 9 — **every landmark heading appears exactly once** — and it is worth more than the tag count it replaces. |
| 2026-09-09 | **D117** | **A `.wu` in the page flow fills its column; a `.wu` on a SIMSCREEN stage does not.** The recreated windows were authored at fixed pixel widths (620&ndash;800 px) back when they were drafted against a narrower column. In a 1218 px column that stranded **~460 px of empty space** beside every terminal, regedit and file-listing block, while the finding boxes beneath them ran the full width &mdash; so the page read as ragged. The distinction is what the window **is**: on a stage it is a window sitting on a recreated desktop, and a centred window is what a desktop looks like, so the stage keeps sizing it; in the page flow it is a **content block** and belongs to the same grid as a table, a `<pre>` or a finding box. Seven windows across `P01` and `P02` had their inline widths removed. The stepped-diagram cap moved 790 &rarr; 960 px in the same pass, so figures do not sit stranded either. | Ebrahim, pointing at the artifact-line window on `P01#p14`: *"why do you leave empty space on the right when there is room &mdash; widen it so it looks right."* The measurement made it plain: `.wu` 760 px, `.finding` 1218 px, same column. Worth recording because the fix is a **distinction, not a number** &mdash; a blanket "windows fill the column" would have stretched the SIMSCREEN's window across its desktop and broken the one place the narrow width is correct. |
| 2026-09-09 | **D118** | **`P03` &mdash; Data at byte level &mdash; is built, and `EVS-05` and `EVS-10` were materialised to build it.** 22 screens &middot; 9 stepped figures &middot; 2 SIMSCREENs &middot; 107 Arabic blocks &middot; the 11 package documents. Both gates pass; all 9 figures step with a changing caption; **9 of 9 SIMSCREEN pointers land on their declared target**. The page turns on one sentence &mdash; *the name of a file is a label someone typed; the bytes are the file* &mdash; and it adds two things INE never covers, both load-bearing later: **endianness**, taught as *pattern versus value*, without which nothing in `P08`'s PE tables can be read; and **what a rename actually changes**, which is the mechanism the whole topic rests on. | The evidence had to be **generated first**: `EVS-05` and `EVS-10` existed only as scripts, and `make_evs10.py` needed a package that was not installed. Both were run and read before a word was written, because `D94` requires a finding to be real &mdash; so the five lying extensions, the GPS pair and the two timestamps on the page are all measurements. **The best thing on the page was already in the repo and nobody had noticed:** `CASE-01B`'s `policy_extract.txt` **passes SHA-256 and fails MD5**, and a file whose content changed fails *both* &mdash; so the split result is logically incompatible with a changed file and the finding is about a manifest. That is reasoning rather than recall, it was written weeks ago for a different session, and it is now the sharpest moment in `T04`, the hardest-graded homework entry and quiz Q5. |
| 2026-09-09 | **D119** | **Every page follows one fixed shape, taken from the previous instructor's decks: named PARTS, section-divider screens, a &ldquo;where we are&rdquo; orientation, numbered sequences, and PLAIN DESCRIPTIVE TITLES.** Spec in `design/page_structure.md`. The six devices `Session 1.pdf` and `Session 2.pdf` use and our pages did not: named parts (*Part 1: Introduction &middot; Part 2: Data acquisition &middot; LAB time*), bare divider slides (*Fundamentals · Digital Evidence lifecycle · Analysis steps*), a lifecycle diagram marked **We are here**, numbered sequences (`#1`&hellip;`#5`), plain titles (*What is acquisition?*, *What are the magic bytes?*), and a **bottom-up build** — that deck reaches magic bytes at slide 15, after digital data, ASCII, representation, storage, allocation, deletion, file structure and file header. **`P01`, `P02` and `P03` are restructured to this shape.** | Ebrahim, after reading all three: *&ldquo;I feel lost in them &mdash; the titles, the way you explain, the order of the sections. Where did you get this from? Look at the instructor material, it is genuinely clear. Every topic feels like a new series.&rdquo;* He is right and the diagnosis is precise. My titles were **editorial headlines** — *&ldquo;A hash proves less than you think&rdquo;*, *&ldquo;You observed a state, not an event&rdquo;* — which are memorable and **completely un-navigable**: you can find *&ldquo;What are the magic bytes?&rdquo;* in a sidebar in one second and you cannot find the other at all. And `P03` opened **hex on screen 3** having defined neither a byte nor how a computer stores anything, which is the same failure in the content order. The insight is not lost — it moves into the body, the finding boxes and the figure captions, which is where it always belonged. |
| 2026-09-09 | **D120** | **Every heading on `P01`, `P02` and `P03` is rewritten to name its content &mdash; 63 headings and 31 sidebar labels.** `D119` fixed the sidebar; the `<h2>`s were still headlines. *&ldquo;I used the GUI and clicked around&rdquo; is not a method* became **The three forensic principles**; *A hash proves less than you think* became **Evidence integrity**; *An absence is a result* became **When there is no metadata**. Where a screen is one of a numbered set the heading carries the number &mdash; **#1 It does not prove the copy is complete**, **#2 A mismatch does not mean tampering** &mdash; so a student always knows how many remain. The rule is in `design/page_structure.md`: **if you cannot guess what is on the screen from its title alone, the title is wrong.** | Ebrahim, naming one example: *&ldquo;&lsquo;I used the GUI and clicked around&rsquo; is not a method&rsquo; &mdash; what does that even mean? Make it professional and understandable.&rdquo;* He is right, and the honest reading is that those titles were **written to be admired rather than used**. The insight in each of them is not lost &mdash; it is in the body, the finding boxes and the figure captions, which is where a point belongs and a signpost does not. |
| 2026-09-09 | **D121** | **The `D107` spotlight check compares what `place()` COMPUTED, not a rendered frame.** The ring animates into position over 500 ms and `place()` can re-run when a figure scrolls into view, so sampling the rendered ring measured the transition rather than the maths &mdash; the gate failed at 700 px on one run and 1100 px on the next, with nothing wrong on the page (measured directly: 4 px). `place()` now publishes `data-ss-ring`, the ring's **intended** centre, in the same instant it publishes `data-ss-spot`; the gate compares those two. The separate check that the **rendered** ring lands on its declared target is unchanged, because that one is only read once the layout has settled and it is the visual assertion. | Two failed attempts before this: a fixed 700 ms wait (fails when the observer fires late) and a wait-until-the-ring-stops-moving loop (passes while the ring sits at its *old* position and the spot is already new). Both were attempts to time a race instead of removing it. The property being tested was never about a frame &mdash; **the spotlight and the pointer are two numbers computed from the same coordinates, and they must agree at the moment they are computed.** A flaky gate is worse than no gate, because it teaches you to re-run until it passes. |
| 2026-09-09 | **D122** | **A technical term is written in ENGLISH inside the Arabic line — enforced by the gate, and checked across the WHOLE document, not just `<span class="ar">`.** `D76` rule 2 has said this since the bilingual layer was designed, and it was broken about **460 times**: `بصمة` for *hash* and for *signature*, `بايتات` for *bytes*, `بتات` for *bits*, `الترويسة` for *header*, `الوقائع` for *findings*, `الاستنتاج` for *interpretation*, `الحرز` for *exhibit*, `الأثر` for *artifact*, `بيان البصمات` for *manifest*, `البيانات الوصفية` for *metadata*, plus `النزاهة` / `الهوية` / `الهيكس` / `الاستحواذ`. **Two mechanical traps, both paid for.** (a) A substring replace corrupts good Arabic — `تُثبت` becomes `تُثbit` and `بترتيب` becomes `bitرتيب`; matching is whole-word with an Arabic-letter lookaround, always. (b) Swapping a noun **breaks agreement**: `البصمة الرقمية` → `الـ hash الرقمية` leaves a feminine adjective stranded on an English masculine noun, and `فتتطابق البصمتان` has a dual ending nothing can carry — five of those were fixed by hand after the sweep. `density_gate.py` now fails on any banned term. | Ebrahim: *“any word that counts as a technical term, use it in English — you cannot write بتات or البايتات.”* He is right and the rule was already written; what was missing was a **check**. The sharpest part is where the violations hid: about **60 of them were in `<script>`** — the SIMSCREEN and `Dgm` caption strings — which the first sweep never touched because it matched `<span class="ar">`. Those strings ship to the student exactly like body text. The gate now reads the **whole file** for this check, and that scope choice is the actual lesson: a content rule enforced on one container is enforced nowhere. |
| 2026-09-09 | **D123** | **`P01` and `P02` are restructured to the `D119` shape, and the “where we are” lifecycle is a real figure on all three pages.** `D119` claimed all three were done; only `P03` was. `P01` and `P02` had `is-part` markers sitting on **content screens** — a sidebar that announced a part and then opened a lesson — and their “Where we are” screens were prose with no lifecycle diagram at all. Now: `P01` 21 → **26 screens** with 5 divider screens (Part 1 The forensic method · Lab time · On your own · Part 2 The forensic report · Report stage), `P02` 20 → **25** with 5, and every page carries `dg-lc-pNN`. **The lifecycle marker is now accurate:** `P01` marks no single stage — it is the map, and a band under all five says so — `P02` is **stage 2 PRESERVE**, and `P03` moves from the stage 2 it wrongly claimed to **stage 3 ANALYSE**. A new gate check asserts `divider screens == is-part sidebar entries`, so the two can never drift again. | The `P03` marker is the part worth recording. It said *stage 2*, defended in its own caption as “still preservation work, because you cannot preserve what you cannot identify” — which is a nice sentence and it made `P02` and `P03` claim **the same stage**, which empties the device of its only job. A “you are here” that does not move is decoration. Signature analysis and metadata analysis are analysis; saying so makes the three pages read as a progression, which is what Ebrahim asked for when he said each topic felt like a new series. |
| 2026-09-09 | **D124** | **One right edge. A page is a COLUMN, the COLUMN is the reading measure, and every block in the flow fills it.** Measured on one screen of `P02`: the `<h2>` ran to **1476 px**, a `.finding` box stopped at **1298**, and the paragraph between them stopped at **1086** — three right edges, none of which is a bug on its own. The cause is that `ch` resolves against the **element's own font-size**, so `110ch` on a 18.56 px box and `92ch` on 18.56 px prose give different pixels, and a box with smaller type gets *narrower* from a *larger* number. A shared edge cannot be expressed per element; it has to be a shared pixel value, and the only element that can own one is the column. So `.page-layout` narrows 1860 → **1560 px**, the column lands at **1176 px** (~100 characters), and every per-element `max-width` inside `.page` is reset to `none`. Two exemptions, re-asserted after the reset: `.divider-inner` (a centred title card) and `figure.photo` (a natural size that stretching would blur). **The render gate now asserts the result**: every flow child of the active screen ends within 2 px of every other. Verified across `P01`+`P02`+`P03`, every screen, 1920/1400/1100 — zero ragged. | Ebrahim, for the **third** time on the same underlying thing: *&ldquo;why do you leave empty space on the right&rdquo;* → `D117` (windows), *&ldquo;widen it&rdquo;* → the measure, and now a screenshot of a `.finding` box short of the paragraph beside it. Three fixes to three symptoms, because each time I treated it as *this element is wrong* instead of asking what the page's edges are made of. The general fault is fixing **the element the user pointed at** rather than the rule that produced it — the same shape as `D116`, where I verified the symptom I had been looking at instead of the property that mattered. The gate check is on the **rendered result**, not on the CSS, because the CSS was never wrong: it was three correct rules that could not agree. |
| 2026-09-09 | **D125** | **One build prompt for the remaining eleven pages: `design/prompts/PAGE_BUILD_PROMPT.md`.** Paste it as the first message of a new session and name the page. It carries: the fixed page shape and the **lifecycle stage per page** (so no two pages claim the same one) · the title rules and the definition rule · the six bilingual rules with the banned-term table and the **two mechanical traps** in fixing terminology · the visual contract (stepped diagrams, SIMSCREEN, photographs, *a picture must earn its place*) · the evidence-first rule · the order of work with the **outline gate** · both gate commands · **ten traps already paid for**, each with the lesson rather than the fix · and a per-page table saying what is buildable now (`P06`) and what is blocked on `staging_plan.md` Phases C/D. The four session-era prompts moved to `_superseded/`. | Ebrahim: *“write me a prompt for the rest of the parts — you look tired from all the corrections — but most important, put the latest changes into the instructions so the prompt is very strong.”* The instruction inside the request is the right one: **a prompt is only as strong as the instructions it points at**, so `Part 9`'s contract gained seven rows and a table of what the gates now assert *before* the prompt was written. The prompt's most valuable section is the trap list, because every entry there is a defect that shipped once and cost a round trip — and the meta-lesson at the end of it (*three separate fixes for “empty space on the right” because each time the element he pointed at was treated as the bug instead of the rule that produced it*) is the one that would have saved the most time if it had been written after the first occurrence. |
| 2026-09-09 | **D127** | **`P04` &mdash; Volatile evidence &mdash; is built, and it is the first page whose Tier 1 evidence does not exist yet.** 29 screens &middot; 5 dividers &middot; **6 stepped figures** &middot; 2 SIMSCREENs &middot; 222 Arabic blocks. Both gates pass. The page turns on one sentence &mdash; *the evidence is disappearing while you decide, and part of what you collect is you* &mdash; and `FIG P04-F2` is the argument: your collector writes a process, a driver load, a prefetch entry and **a USBSTOR key that looks exactly like the exfiltration device `T17` will ask you to find**. **The evidence decision:** `EVS-03` and `EVS-09` are ⛔ PENDING on `staging_plan.md` Phases C/D, and no public memory image could be fetched here &mdash; the network policy refused the corpora hosts and the local VM could not fetch a browser engine either. Rather than invent Volatility output, `T06` and the **acquisition** half of `T07` are built at full depth (they are procedure, not corpus), and the 15-minute **investigation** routes to the free TryHackMe Volatility room, already the vetted spine in `practice_platforms.md`. The References screen says so on the page. | Two things are worth recording. **(a) The blocked-evidence rule needed a finer edge than *the page does not start*.** Splitting the topic by *what actually needs a corpus* recovered 45 of 60 minutes at full quality; the rule as written would have deferred the whole page for weeks. **(b) The page's best content came from the currency check, not from the courseware.** Verifying the tools against primary sources found that WinPmem now writes **RAW only** (its AFF4 output was never carried to the new driver), that **DumpIt is now Magnet's**, and that Volatility 3 has **no profiles at all** &mdash; while our courseware still teaches `imageinfo` and `--profile=Win7SP0x64`. That became `FIG P04-F4` and the rule the page ends on: **RFC 3227 is from 2002 and is still exact; a command line from 2019 is already wrong. Principles age well and procedures do not.** |
| 2026-09-09 | **D126** | **The Arabic must name the SAME technical term as its English &mdash; and the check that asserts it was WRONG the first time.** The `D122` sweep mapped one Arabic word to one English term, but `بصمة` means **two** things &mdash; *signature* (magic bytes) and *hash* &mdash; so ten blocks on `P03` ended up saying *hash* under an English line about signatures. Neither side is illegal alone, which is why the `D122` check cannot see it: **the pair is what is wrong.** Fixed, and `density_gate.py` now checks six term pairs. **The check itself is the lesson.** Version one used `re.finditer` over `<(p\|li\|div\|td)>` &mdash; and `finditer` scans forward from the end of each match, so an outer `<div>` wrapping a table **swallowed every row inside it** and the rows were never examined. It passed a page I had deliberately broken. Version two walks each `.ar` span and reads the English immediately before it, which is the `D108` relationship anyway. | I nearly shipped a gate that proves nothing, one turn after writing *&ldquo;a gate that only ever passes proves nothing &mdash; break the page on purpose and confirm the check fails&rdquo;* into the build prompt. The mutation test is the only reason it was caught, and it took **thirty seconds**. Two conclusions: a regex is the wrong tool for a containment relationship in nested HTML &mdash; the outer match hides the inner ones silently, with no error &mdash; and **the rule about mutation-testing gates applies to the gate you are writing right now**, not only to the ones you inherited. |
| 2026-09-09 | **D128** | **Five defects Ebrahim found on `P04`, and the rule each one produced.** (a) **A one-step figure has a Play button that does nothing.** The `Where we are` lifecycle had a single step, so the control bar rendered `1 / 1` and Play was inert. It is now **five steps, one per stage**, with the page's own line last &mdash; shown first it sat over a strip where only `ACQUIRE` was lit, which is wrong for every page but `P04`. Applied to `P01`&ndash;`P04`. (b) **SVG box labels are `text-anchor="middle"`.** Left-anchored labels with a `WE ARE HERE` line under them read as ragged; `check_widths()` is now anchor-aware, since a middle-anchored line spills **both** ways. (c) **`viewBox` height is guarded like width.** `FIG P04-F1`'s closing band was **clipped and invisible** &mdash; it rendered, it just was not there. The build now fails on any element whose bottom passes the viewBox; mutation-tested. (d) **A divider's `h2` must not carry the accent bar.** `h2::before` paints a 4 px rule at the h2's left edge, and on a centred 900 px title card it landed far from the words, reading as a broken render. `.divider-inner h2{border:0}` did not remove it &mdash; **it is a pseudo-element, not a border.** (e) **`RFC 3227` has SEVEN tiers, not eight**, and memory sits in tier 2 **with** the network structures. I had split that tier in two and put network above memory &mdash; which contradicted our own lab, where the collector takes memory first. One tier now, memory named first inside it, with the reason on the figure: *the process list and the connections can be rebuilt from a memory image; nothing rebuilds memory.* | Ebrahim, listing all five in one message and then the sixth: *&ldquo;the Arabic sometimes goes really bad &mdash; I do not understand what you mean.&rdquo;* That one matters most and it is **the opposite of the failure `D104` fixed**. `D104` moved the Arabic off Egyptian colloquial into MSA; I then drifted into **literary** MSA &mdash; `لطخة لا لقطة` for *a smear, not a snapshot* is correct Arabic and communicates nothing to a working analyst. `D76` rule 3 now carries a do-not-write table and one test: **not *is this correct Arabic* but *does a reader get it at normal speed, once*.** Swept across all four pages. |
| 2026-09-09 | **D129** | **`P05` &mdash; Acquisition &mdash; is built: 31 screens, 5 stepped figures, 2 SIMSCREENs, the longest page in the diploma.** It turns on two sentences. **(a)** `verified` describes a **write-then-read round trip inside one tool** &mdash; the source is never read a second time, so the word says nothing about the disk, nothing about whether a write blocker was attached, and nothing about whether the right device was selected. **An image of the wrong disk verifies perfectly.** **(b)** One disk can report **three different sizes**: `IDENTIFY_DEVICE` 100,000,000 &middot; `READ_NATIVE_MAX` 156,301,488 &middot; DCO 625,142,447 &mdash; INE's own worked example. Image the first number in good faith and a fully verified forensic image covers **16% of the drive**, with nothing anywhere saying so. The operational consequence is a fixed order: **DCO before HPA**, because a DCO lowers the native maximum `hdparm -N` measures against, so the wrong order reports *&ldquo;HPA is disabled&rdquo;* on a disk that is hiding capacity. **Evidence:** `EVS-02`/`EVS-04` are pending, so `T09` runs against **a device the student owns** &mdash; and for an acquisition exercise that is better than realism, because knowing ground truth is the only way to tell the tool's mistake from your own. **Tool currency:** FTK Imager is now **Exterro 8.3** with a separate paid **Pro** line, not AccessData 4.x; `dc3dd` is still packaged in Kali at 7.3.1. | Two process notes. **A table with Arabic in every cell does not survive more than two columns below 1200 px** &mdash; four tables starved on this page and the fix was structural every time, so that is now a rule rather than a repeated repair. And **I overwrote `P04` with `P05`**: the new page's assembler was copied from `P04`'s with a `sed 's/p04/p05/'`, which does not match `page-04`, so the output path stayed pointed at the previous page. It was caught immediately by reading the `<title>` of what had just been written, and `P04` was regenerated from its own generator in one command &mdash; **which is the only reason it cost nothing.** Every page being reproducible from a script is what turned a destroyed deliverable into a thirty-second rebuild. |
| 2026-09-09 | **D130** | **`P06` &mdash; Hidden information &mdash; is built, and it is the first page in the diploma with NO evidence dependency.** 26 screens &middot; 4 stepped figures &middot; 1 SIMSCREEN &middot; 1 real rendered image. `EVS-10` was regenerated and **read before a word was written**, so every number on the page is a measurement: `team_photo.jpg` carries a ZIP local header at offset **10,296**; `receipt_scan.png` differs from its clean twin in **190 of 76,800 pixels, all in the blue channel** (R:0 G:0), and the recovered payload reads `MRG-INTERNAL: staged archive is customer_export.7z`; `office_floor3.jpg` names **three different days** across `DateTimeOriginal`, `DateTime` and its file mtime; and `invoice_batch.jpg` is redacted at 600&times;400 with a **1,735-byte EXIF thumbnail that is not**. The page's turning sentence: **a file can be exactly what it claims to be and still be carrying something else** &mdash; `team_photo.jpg` *passes* the `T04` signature check, which is precisely why the check after it exists. | Two things worth recording. **(a) I rendered the thumbnail pair and looked at it before claiming the redaction failed.** The claim *&ldquo;the thumbnail is not redacted&rdquo;* is the whole screen; asserting it from the generator's intent rather than from the pixels would have been exactly the failure this project is built to prevent. The black box is on the main image and absent from the thumbnail &mdash; verified visually, then written. **(b) The `D129` two-column rule held on first contact:** three tables starved their Arabic at 1100 px and the fix was structural each time. One of those fixes was to **delete** Arabic rather than restructure &mdash; a two-word deliverable column (*&ldquo;a four-row table&rdquo;*) needs no explanation, and `D76` rule 6 already said so. **The bilingual layer is not improved by translating things that carry no idea.** |
| 2026-09-09 | **D131** | **`P07` &mdash; Malicious files &mdash; is built WITHOUT a malicious sample, and that is a scope decision, not a workaround.** 27 screens &middot; 5 stepped figures &middot; 1 SIMSCREEN. `T11`'s first two parts (50 of 75 minutes) are **format analysis** &mdash; they need a document and a binary, not malware &mdash; and they are built in full against two benign artifacts read before writing: an authored `handover_notes.docx` (3,111 B, five parts, `word/document.xml` only **257 B**, no `vbaProject.bin`) and a **published** PE, Pillow 12.3.0's `_imagingmorph.cp312-win_amd64.pyd` (12,288 B, `e_lfanew` **0x110**, AMD64, 6 sections, `TimeDateStamp` &rarr; **2026-07-01 10:09:12 UTC**, 30 imports reaching **no** socket, registry, process or file-enumeration API). `T11`'s third part needs `EVS-06` and is **not substituted**: no maldoc is generated to teach against, and none is needed, because the half of the topic that transfers to every future file is the structural half. **The best thing on the page is a measured entropy scale** &mdash; zeroes 0.00 &middot; whole PE 5.08 &middot; `.text` **5.99** &middot; the same PE compressed **7.96** &middot; random **7.95** &mdash; which makes the real lesson provable: **entropy cannot distinguish compressed from random, and certainly not either from malice. It is a reason to look, never a finding.** | The page's summary line is the general form of everything on it: **every over-claim in malware triage is one adjective past the evidence.** *A VBA project is present at `word/vbaProject.bin`* is reproducible with `unzip -l`; *the document contains a malicious macro* adds a word the container cannot carry. The exercises are built on that one move, and the Section 7 screen is a do-not-write / write table of four real examples. Worth recording separately: the `.text` entropy of **5.99** is the number a student should carry &mdash; teaching what **normal** measures, on a file known to be ordinary, is what makes an abnormal reading later *recognisable rather than alarming*. Most courses teach the threshold and never show the baseline. |
| 2026-09-09 | **D132** | **The terminology gate was matching the wrong thing, and eight pages were clean by it and were not.** `D122`'s check anchored on `(?<![Arabic letter])TERM` and listed the bare and *al-* forms of each term by hand. Arabic **glues its particles to the front of the word** &mdash; and *li* + *al* contracts to *lil* &mdash; so `للترويسة` has a letter immediately before the stem and walked straight past a gate that had **already been run on that page and passed it**. Prefixes are now part of the pattern, not of the dictionary, because a list of forms will always be one form short. Allowing prefixes on everything then **over-corrected**: `أثر` is the translation we ban for *artifact* **and** the ordinary Arabic word for *effect*, so *its effect is long* was reported as a terminology break. The dictionary is now **two classes** &mdash; **STEMMED** (never anything but the technical term: `بصمة`, `بايت`, `ترويسة`, `بتات`, `الهيكس` &mdash; prefixes **and** suffixes match, including the *ta marbuta &rarr; ta* shift in `بصمتها`) and **LITERAL** (an ordinary word too &mdash; only the exact definite form matches). **Fourteen real violations across all eight built pages**, including `البصمتان` and `بصمتي` &mdash; the stranded duals `D122` predicted and did not catch. All eight pass now. | The gate passed `P08` twice before I read the line it was passing. **A gate you wrote yourself is evidence about the gate, not about the page**, and the mutation test is the only thing that separates them: the first version of the two-class fix caught three of four planted violations, and the fourth &mdash; `بصمتها` &mdash; is the exact morphological trap `D122` had already written down and I had not encoded. The general fault is the same one as `D124`: I keep fixing **the instance**, and a violation list is an instance of the rule *Arabic attaches its particles to words*. |
| 2026-09-09 | **D133** | **`P08` &mdash; The disk &mdash; is built, and `EVS-07` was generated, verified and made REPRODUCIBLE to build it.** 24 screens &middot; 5 dividers &middot; 5 stepped figures &middot; 2 SIMSCREENs &middot; `T12` + `T13`, 113 topic minutes. Both gates pass. Every number is read out of a disk: cluster size **4,096** from the BPB &middot; five files at 1 / 100 / 4,096 / 4,097 / 10,000 B giving slack of **4,095 / 3,996 / 0 / 4,095 / 2,288** &middot; **18,294 B of files occupying 32,768 B, so 14,474 B of slack on a volume with five files on it** &middot; `MEMO.TXT` 20 B at offset `0x13000` with **4,076 B of a deleted salary document** behind it &middot; the MBR table written **by hand in Python** because `fdisk` and `sfdisk` were both unavailable, which shows the sixteen bytes instead of a tool's summary of them &middot; GPT header and array **CRC32 recomputed and matched**, backup header at LBA **131,071**. **Case 04 is the best evidence work in the project so far:** LBA 0 zeroed &mdash; **512 of 67,108,864 bytes, 0.0008%** &mdash; a naive scan returns **three** boot-sector candidates for **two** partitions, and `BPB_BkBootSec` = 6 identifies the third as the FAT32 volume's **own backup**. Discard it and the rebuilt table is **byte-identical** to the original. | Two things worth recording. **(a) The failure-then-correction is the lesson, not the recovery.** A table built from all three candidates also mounts &mdash; so *the volumes mount correctly* is a statement about plausibility and nothing else, and the report stage asks for the **discarded candidate** in writing, because that sentence is the whole difference between a forensic recovery and an IT repair. **(b) `make_evs07.py` was not deterministic and the manifest would have been a lie.** Five independent sources of drift &mdash; the FAT volume serial, `mcopy`'s timestamps, the **timezone** (FAT stores local time), the volume-**label** entry `mkfs.fat` stamps, and `sgdisk`'s three random GUIDs that both CRC32 fields then cover. All pinned; two builds are now byte-identical across all four images. Before the fix they differed **in as little as two bytes**, which is exactly the amount that makes a hash manifest worthless. Finding that required regenerating the set and diffing it &mdash; **which is the thing `EVS-10` claimed as *determinism proved twice* and this set had never actually been asked to do.** **(c)** One more gate check came out of this page: `partitionان` &mdash; an Arabic **dual ending glued to an English noun** &mdash; shipped through both gates and was found by looking at a screenshot. It is the same shape as `D122`'s `تُثbit` corruption in the other direction, and neither is a *banned term*, so nothing was looking for it. The density gate now fails on **any join between the two scripts** in either order; it found one more on `P06` immediately (`hashاته`). The house style always puts a space between the scripts, so the check has no legitimate exception. |
| 2026-09-09 | **D134** | **A wide comparison figure is `class="photo wide"`, not `class="photo"`.** `figure.photo img` is a fixed **560 px**, which is the right size for a photograph of a write blocker and the wrong size for a **3:1 side-by-side**: at 560 px each half is 270 px and <b>the thing the figure exists to show is not visible</b>. `.photo.wide` fills the column (so it also shares the one right edge `D124` asks for) and drops the 560 px cap on its caption. Applied to `P09`'s carve comparison and to the **two figures on `P06` that have been shipping squeezed since that page was built**. | Found by looking at a screenshot of a new page, not by a gate &mdash; and the interesting part is that the render gate **passed it**, correctly: `figure.photo` is on the ragged-edge exclusion list (`D124`) because a photograph has a natural size that stretching would blur. The exclusion was right for photographs and I never asked whether everything wearing that class was one. **A rendered comparison is a diagram that happens to be a raster**, and it belongs to the column like a table does. The lesson is about exclusion lists: every entry on one is a claim about a category, and a class name is not a category. |
| 2026-09-09 | **D135** | **`P09` &mdash; File systems &mdash; is built, and it was never blocked.** 29 screens &middot; 6 dividers &middot; 6 stepped figures &middot; 2 SIMSCREENs &middot; 1 rendered result. Every planning document had `P09` **⛔ blocked on `EVS-02`**; the `D127` split says the topics need **a** file system to read, not **the suspect's**, so `EVS-11` was built: an NTFS volume, a FAT32 volume with a **deliberately fragmented** file, and the same FAT volume with 0.2051% of it zeroed. **49 assertions** passed against the produced images before a word was written. The page turns on two sentences &mdash; *deleting a small NTFS file does not remove it from anywhere*, because a resident `$DATA` lives inside the very record the delete marks free (**record 67: flags 0x00, links 0, seq 2, and 212 bytes still readable at `0x14D78`**) &mdash; and *a file that opens is not a file that is intact*, because the fragmented photograph carves as **112,253 bytes instead of 75,389**, exactly **+36,864** of somebody else's log file, and **still opens as an 800&times;500 JPEG**. | Three things worth recording. **(a) `mtools` will not fragment a file** &mdash; it allocates from a next-free hint &mdash; so `whiteboard.jpg` was written into the FAT **by hand**, chain and directory entry both, and then proved real by walking the chain and hashing the reassembly against the original. The first attempt hard-coded cluster 19 and **silently overwrote the deleted file placed there a moment earlier**, deleting the artifact the carving lesson depends on; the layout is now computed from the volume. **(b) The NTFS image is deliberately NOT byte-reproducible.** 344 bytes of 67,108,864 differ between builds because `mkntfs` writes a `$LogFile`, an `$MFTMirr` and index entries carrying timestamps &mdash; and pinning those means **forging a journal that does not correspond to what happened**, which is a worse thing to hand a forensics student than an unstable hash. The manifest carries the **four MFT records the page reads** instead, which are identical across builds. Honest irreproducibility, named, beats reproducibility bought with a lie. **(c) I nearly shipped two fabricated hashes.** Writing the manifest entry I completed two SHA-256 values **from the 16-character prefixes the build had printed** instead of copying `EVS-11.sha256`. Caught by re-reading, corrected, and then every 64-hex string in the `EVS-07` **and** `EVS-11` sections was checked mechanically against the real manifests &mdash; both clean now. The rule: **a truncated hash is not a hash, and it is never completed from memory.** In a project whose entire claim is that the numbers are measured, this is the one class of error that would have been worth more than all the others put together. |
| 2026-09-09 | **D136** | **Page shape v2 &mdash; every page from `P10` on, and `P01`&ndash;`P09` in the review pass.** Ebrahim's brief, in order: *a simple introduction tied to the previous topic &rarr; the theory, each part with a practical picture of it &rarr; the practical for the instructor first &rarr; then for the student, alone or as a team &rarr; a cheat sheet &rarr; a task &rarr; references.* So `LAB TIME` splits into **INSTRUCTOR DEMO** and **YOUR TURN**, a **CHEAT SHEET** screen becomes mandatory, `ON YOUR OWN` and `HOMEWORK` merge into **TASK** (which carries the report stage and a grading line), and `Where we are` gains one paragraph that says what the previous page gave us. Spec in `design/page_structure.md`. Every v1 device &mdash; dividers, lifecycle figure, plain titles, the definition rule &mdash; is unchanged. | Ebrahim, giving the review brief before the last pages were built: *“the student must understand a simple introduction to the topic and its link to the one before, then the theory with the practical picture, then the practical for me first, then the student alone or as a team, then the cheat sheet, then a task, and references last.”* Building `P10`&ndash;`P14` in the old shape and then rebuilding them would have been the wrong order of work, so the shape changed first. The cheat sheet is the one addition that costs nothing and that every student will actually use: it is the screen they keep open in the next lab. |
| 2026-09-09 | **D137** | **`P10` &mdash; The registry &mdash; is built on Tier 2 evidence, and the Tier 2 path now has a fetcher.** `P10`, `P11` and `P12` were marked blocked on `EVS-02`. Published corpora exist for all three &mdash; Eric Zimmerman's test hives and artifact files (MIT) and Samir Bousseaden's `EVTX-ATTACK-SAMPLES` (GPL-3.0) &mdash; and `scripts/fetch_corpora.py` fetches them **at pinned commits** and refuses to finish unless every file matches the recorded SHA-256, so upstream cannot move under the pages. `EVS-12`: 39 assertions. **The page's best finding was in the data, not the plan:** the ADATA drive's “First InstallDate” is the **latest** of four dates &mdash; seven months after the user last mounted it and eight months after its volume GUID was minted (UUIDv1 clock, **2014-07-10**) &mdash; because Windows reinstalled the driver instance ten seconds after another drive was removed. **RegRipper 3.0 was installed and run on the real hives, and the demo SIMSCREEN quotes its output verbatim.** | Two notes. **(a)** The corpus is not one case &mdash; `SYSTEM` and `NTUSER.DAT` share a machine (43 volume GUIDs in common), `SOFTWARE` and `SAM` do not. The page says so where it matters and makes it a habit: *prove the hives are from one machine before you join them.* **(b)** The user hive is a real person's profile, published by him for this purpose; the page uses only its device keys and displays nothing that describes him. That is the line between Tier 2 and `R9`, and it is drawn per key, not per file. |
| 2026-09-09 | **D138** | **`P11` &mdash; What ran, what was touched &mdash; is built on `EVS-13`, and the whole page turns on ONE MOVE: each artifact supports one verb, and you refuse to upgrade it.** 25 screens &middot; 7 dividers &middot; 6 stepped figures &middot; 1 SIMSCREEN &middot; `T18`+`T19`, 78 minutes. `EVS-13` is ten published parser-test files (Eric Zimmerman, MIT) fetched at pinned commits; **40 assertions**. Every number is real: prefetch **run count 55** and eight start times from `libscca`; ShimCache **350 entries** with `PowerShell.exe` carrying its **file mtime, not a run time**; UserAssist **43,392 seconds of focus** on Visual Studio; a shellbag tree on a **vanished J:** drive; two `$I` records deleted **eight seconds apart**. RegRipper 3.0 was run on the real `UsrClass.dat` and the shellbag SIMSCREEN quotes it verbatim. | The verb discipline is the pedagogical spine, and it is the general form of every earlier &ldquo;finding vs interpretation&rdquo; screen: **started / was present / was launched / browsed / binned** are five different claims from five mechanisms, and the single most common error in execution analysis is writing &ldquo;ran&rdquo; on a ShimCache line whose date is a file&rsquo;s mtime. Two scope calls worth recording. **(a)** A drive **letter** is not a **device** &mdash; the page refuses to equate its shellbag J: with `P10`&rsquo;s ADATA J:, and makes that refusal a caveat screen rather than a footnote. **(b)** Amcache and live VSS need a full image, so they are taught here and **run in `P14`**; teaching an artifact one page and running it another is now an accepted pattern when the evidence tier cannot support the lab. |
| 2026-09-09 | **D139** | **A backslash in a SIMSCREEN or figure caption broke the whole page&rsquo;s JavaScript, and the shared builders now escape it.** `EXAMPLE\user01` in a caption string reached the inline `<script>` as `\u...` &mdash; an <b>invalid Unicode escape</b> &mdash; and every stepped figure and SIMSCREEN on `P12` failed to initialise. The render gate caught it as 100+ `pageerror` findings, which is exactly what that check is for. `lib.py`&rsquo;s `fig()` and `sims.py`&rsquo;s `simscreen()` escaped only single quotes in the `en`/`ar` strings; they now escape backslashes first. | `P10` and `P11` had Windows paths in captions too, but none produced a lowercase `\u` &mdash; `\U`, `\w`, `\t` are harmless in a sloppy-mode string, only `\u` and `\x` throw. `P12` is the first page whose evidence is full of `DOMAIN\user` strings, so it is the first to hit it. The lesson is the same as `D122`&rsquo;s script-scope one: <b>a caption is code, and any character special to the target language has to be escaped, not just the quote that happened to bite first</b>. Fixed in the builders, so `P13`/`P14` inherit it. |
| 2026-09-09 | **D140** | **`P12` &mdash; Windows event logs &mdash; is built on six real attack recordings, and it turns on the idea that clearing a log is the loudest event in it.** 22 screens &middot; 6 dividers &middot; 5 stepped figures &middot; 1 SIMSCREEN &middot; `T20`, 49 minutes, single topic. `EVS-14` is `EVTX-ATTACK-SAMPLES` (GPL-3.0), six `.evtx` fetched at a pinned commit; **29 assertions**. Every event is real: a **1102** clear by `EXAMPLE\user01` inside a 112-record file (110 of them 4663 noise); a **104** clear 42 seconds earlier; **Guest (RID 501) added to Administrators**; a **type-2** logon that is a Chrome updater, not a person; the **type-9 + seclogo + ::1 + 4672** pass-the-hash pattern wrapped in a clear; and a **Sysmon 13** run-key persistence disguised as `taskhost`. Every finding is written as EventID + Provider + UTC + the meaning-bearing field, and mapped to MITRE (T1070.001, T1550.002, T1098, T1112). | Two teaching choices. **(a)** The page is aimed at students who have never opened Event Viewer for an investigation, but Ebrahim&rsquo;s own students are SOC analysts who know these IDs &mdash; so the framing is explicit: *here you read them as a forensic examiner writing for court, a stricter standard than an alert.* **(b)** The pass-the-hash screen is the first on any page where the finding is a **pattern across five events, none of which means it alone** &mdash; the general form of every &ldquo;finding vs interpretation&rdquo; screen, and the moment the diploma asks the student to name a shape rather than read a field. The `Security-log-vs-Sysmon` screen carries the humility rule: *absence in one log is a fact about the logging, not about the attack.* |
| 2026-09-09 | **D141** | **`P13` &mdash; Network evidence &mdash; is built on one synthesized capture that carries a whole intrusion, and it is the first page whose three topics are one story.** 25 screens &middot; 7 dividers &middot; 4 stepped figures &middot; 1 SIMSCREEN &middot; `T21`+`T22`+`T23`, 90 minutes. `EVS-08` is a 22-packet, 6-minute pcap plus its proxy log and phishing email, built with scapy and read back with tshark; **20 assertions**. The story is measurable end to end: a phishing email (SPF/DMARC fail, Reply-To a raw IP) links to `sync-check.net`; the victim **beacons there every 30 seconds with zero jitter** using a 2001 IE6 User-Agent; and the stolen client list leaves **three ways** &mdash; a DNS-TXT tunnel whose Base32 labels decode to the secret, a plain-HTTP POST, and the beacon itself. **`Received`, `Reply-To` and the POST destination are one IP** (`185.220.101.7`), which is the strongest fact in the case and takes both the pcap and the header to see. | Two things worth recording. **(a) Tier 3 is honest for a pcap and dishonest for a hive, and the page says why on its own evidence note:** a capture of traffic we specified is a real recording with ground truth we control; a fabricated `.evtx` is a lie about what an artifact looks like. This is the first page to lean on that distinction, and it is the same line `EVS-10` drew for synthesized images. **(b) The teaching spine is *metadata over content*:** the beacon is found by **timing**, the tunnel by **shape**, the forgery by **header fields** &mdash; all of which survive encryption, which is the truth about modern network forensics where most payloads are TLS. The page ends by pointing back at `P09`/`P11`: the C2 URL is also in browser history and a LNK file, and two independent sources for one fact is what stage-4 VALIDATE is made of. |
| 2026-09-09 | **D142** | **`P14` &mdash; Timelines and the final report &mdash; is built, and the eCDFP diploma is COMPLETE: 14 of 14 pages live, all passing both gates.** 21 screens &middot; 6 dividers &middot; 4 stepped figures &middot; 1 SIMSCREEN &middot; `T24`+`T25`, 85 minutes. This is the capstone, and it is the only page that sits at **two** lifecycle stages &mdash; the super-timeline **is** VALIDATE, the report **is** PRESENT. `EVS-09` is an eleven-row plaso-style super-timeline built to be **consistent with the real sets already made** (`EVS-08`'s C2 and IP, `EVS-12`'s host and timezone), so every row traces to the page that taught its artifact. The incident reads as one sentence: phish &rarr; click (12:24:03) &rarr; execution + Run-key persistence in the **same second** &rarr; beacon (12:24:20) &rarr; pass-the-hash &rarr; three exfil paths &rarr; two log clears &mdash; **and the nine rows above the clears survived because they live off the event logs.** | The page is deliberately **not** a new artifact: it is the discipline every page rehearsed, named once and applied to all of them. The whole diploma collapses to one writing rule &mdash; *state the claim in the verb the evidence supports, cite the artifact, give your confidence, name the limit; never one adjective past the bytes* &mdash; and the capstone assessment is a **peer review** whose only question is whether a claim is one adjective past its evidence. That is the through-line of all 142 decisions: the project's gates, its `finding / interpretation / cannot-prove` encoding, its verb discipline, its mutation-tested checks, and its refusal to fabricate a hive while allowing a synthesized pcap all serve the same end &mdash; a report another examiner can reproduce and no one can push past the bytes. **The diploma teaches one habit of mind; fourteen pages were fourteen rehearsals of it.** |
| 2026-09-10 | **D143** | **The review-and-improve pass has begun, and its rule is one checklist applied to every page, one page at a time.** Ebrahim's brief after the build: review all fourteen topics and improve them on seven dimensions &mdash; shape and delivery, appearance that works, highly visual over theoretical, content strength, heavy simplification in both languages, progressive order from zero, and the strongest content overall. `design/review/REVIEW_PASS.md` turns the seven into concrete per-page checks. The biggest structural item: `P01`&ndash;`P09` were built in shape-v1 and are retrofitted to shape-v2 (INSTRUCTOR DEMO / YOUR TURN split, a mandatory CHEAT SHEET screen, TASK carrying the report stage, and the `Where we are` bridge paragraph); `P10`&ndash;`P14` are already v2 and are checked, not rebuilt. Each page: shape → order → visual → simplify → both gates → a `DECISIONS` row → a short report. | **The images decision, stated once so it is not re-litigated per page.** Ebrahim asked for *real screenshots and images from the web*. The honest, publishable form of that on a public repo (`D22`) is not scraped third-party screenshots &mdash; that is someone's copyright and the project's own `D93`/`D94` forbid decoration. It is: the licensed Wikimedia photo set already vetted in `image_sources.md`, extended where a physical object is genuinely the subject; **SIMSCREENs and terminal captures made from the REAL tool run on the REAL evidence** (RegRipper, tshark and the parsers are already run this way), verified against the THM writeups and INE modules on disk; and images rendered from measured evidence. Every lab is checked step-by-step against the real tool. That is *more real* than a magazine screenshot, and it is the only version that survives the integrity bar the whole diploma is built on. |
| 2026-09-10 | **D144** | **`P01` is retrofitted to shape-v2, and the retrofit is a reproducible transform, not a hand-reorder.** The page was strong content in the wrong shape: theory interleaved with practice (method theory → toolkit lab → judgement drills → report theory → writing drills), no `INSTRUCTOR DEMO`/`YOUR TURN` split, the cheat sheet buried inside the Summary, homework standing apart from the report stages. Now it reads in the canonical v2 order that `P10`&ndash;`P14` already use: **PART 1 method + PART 2 report (all theory) → INSTRUCTOR DEMO** (the instructor seals the toolkit and writes one artifact three ways on the board) **→ YOUR TURN** (the judgement drills, the write-your-own, the knowledge check) **→ CHEAT SHEET** (promoted to its own mandatory screen) **→ TASK** (the old homework, now graded, carrying the two report-stage screens). 26 → **28 screens, 6 dividers**. Content was not rewritten &mdash; it was already good; this was structure. Both gates green: density ALL PASS (28/28 sidebar, 6/6 dividers, 141 Arabic blocks), render **zero findings** across 1400/1100/900/700/480. | Two records. **(a) The reorder was done by `~/build/p01_review.py`: a data-driven reassembler that splits the page into sections, applies asserted string-edits, reorders, renumbers ids, rebuilds the sidebar and the counter, and writes once at the end** &mdash; so a mismatch aborts with nothing written rather than half-corrupting a 984-line file by hand. That is the `D129` lesson applied before the fact: a page you can regenerate is a page you can safely restructure. The same script is the pattern for `P02`/`P03`, the other two hand-written pages. **(b) The `Where we are` bridge already existed** (it ties back to eCIR, the prior course), and the `��` glimpsed in a terminal was a display-font artifact, not corruption &mdash; U+FFFD count in the file is **0**. Verifying that before "fixing" it saved inventing a bug. The render gate can't run on the device (its Chromium download is refused by the egress allowlist), so the page is staged to the cloud container, where Chromium is pre-installed, and gated there &mdash; the standing loop for the whole review pass. |

| 2026-09-10 | **D145** | **`P02` is retrofitted to shape-v2, and the review caught a wrong technical term the build had shipped.** Single-topic (`T03`), so the theory was already grouped (Part 1 what a hash proves, Part 2 preserving the evidence); the work was the practical spine. `LAB TIME` &rarr; **INSTRUCTOR DEMO** (the instructor verifies a set that fails one file) + **YOUR TURN** (the sorting and defect-hunt exercises, the knowledge check); the cheat sheet and the homework, both **buried inside the Summary screen**, are promoted to a standalone **CHEAT SHEET** and a graded **TASK**; the old `Report stage` divider becomes **TASK**; and a **References screen was added** &mdash; `P02` had none, ending instead on a Summary that carried cheat, homework and the forward note all at once. 25 &rarr; **29 screens, 6 dividers**. Both gates green: density ALL PASS (29/29 sidebar, 10/10 SVGs titled, taskList=1), render **zero findings** across five widths. | **The review found a term bug the build and both gates had passed:** the lab screen&rsquo;s Arabic `h2` read *&ldquo;التحقق من cluster”* &mdash; *verify a `cluster`* &mdash; under an English title that says *verify a **set***. `cluster` is a filesystem term (`T13`/`P09`), not a word for a handful of documents, and it is exactly the kind of near-miss the density gate cannot see because neither script is a *banned* term and they are not glued. It was found by reading the divider and the screen side by side, which is what dimension-5 (simplify, stay professional) actually is. Fixed to `مجموعة`, and the manifest wording aligned to `manifest` throughout. The lesson is `D126`&rsquo;s in a third form: a gate proves the pair of scripts agree on *banned* terms; it cannot prove the writer picked the *right* ordinary word, and only a human reading both languages can. |
| 2026-09-10 | **D146** | **`P03` is regrouped to shape-v2, and the regroup fixed a real order bug in the original.** `P03` is two topics &mdash; file identification and metadata &mdash; each built in v1 with its own theory then its own lab, interleaved. v2 groups: **PART 1 (file identification) + PART 2 (metadata) as pure theory**, then **one INSTRUCTOR DEMO** carrying both SIMSCREEN labs (the signature sweep and the metadata read), then **YOUR TURN** (Case 01, the split-manifest challenge, and the knowledge check), then **CHEAT SHEET** and a graded **TASK** (the homework plus the two report-section screens). The cheat sheet, homework and forward note were all crammed into one closing `Reference` screen; they are now three screens, and a real **References list was added**. 31 &rarr; **34 screens, 6 dividers**. Both gates green after one fix. | **The regroup was not cosmetic &mdash; it corrected the teaching order.** In v1 the metadata SIMSCREEN (`ss-t05`, reading `DateTimeOriginal` vs `ModifyDate`) ran at Part 2 #3, *before* the screen at Part 2 #4 that defines what those two timestamps are. Grouping all theory ahead of the demo puts the definition before the use &mdash; the `D80` rule the order gate exists to enforce, here fixed by structure rather than by moving one screen. And the **density gate caught my own new text**: the Task heading read *&ldquo;حدِّد الهوية&rdquo;* &mdash; `الهوية`, the translated form of *identity*, where the page everywhere else keeps the English term `الـ identity`. Fixed. `D76` rule 2 earning its keep on the reviewer&rsquo;s output, not the author&rsquo;s &mdash; which is the whole point of running the gate on every page, retrofit or not. |
| 2026-09-10 | **D147** | **`P04` is regrouped to shape-v2, given the cheat sheet it never had, and trimmed where the current gate showed it was over-dense.** Two topics (live response + memory): theory grouped into **PART 1 + PART 2**, the two guided labs merged into one **INSTRUCTOR DEMO** (collect the volatile state, then image memory with WinPmem), exercises + knowledge check under **YOUR TURN**, a **CHEAT SHEET authored from scratch** (`P04` shipped without one), and the homework relabelled to a graded **TASK** carrying the two report-stage screens. 29 &rarr; **31 screens, 6 dividers**. Both gates green. | **The review caught that `P04` did not actually pass the current density gate.** Regrouping is word-neutral, but adding the missing cheat sheet pushed the page to 4040 words (limit 4000) and surfaced that one existing screen &mdash; *What a memory image does not prove* &mdash; was **276 words, over the 250 cap**, and had been since it was built: the gate was rewritten stricter at `D132`, after `P04`. The fix was not word-shaving but the simplification the page wanted: its `#2` limit box was labelled *&ldquo;The smear again&rdquo;* &mdash; an explicit restatement of `#1` &mdash; so it was **deleted and the list renumbered to three**. Page to 240 worst / 3990 total, both green. The lesson for the rest of the pass: *&ldquo;passed both gates&rdquo; in the build logs meant the gate as it stood that day*, and every retrofitted page is re-gated against today&rsquo;s, which is the stricter one. |
| 2026-09-10 | **D148** | **`P05` is regrouped to shape-v2, and it is the one page the review shows is genuinely OVERSIZED.** Two topics (`T08` what/format + `T09` making the image): theory grouped, the guided steps merged into one **INSTRUCTOR DEMO** (capacity check &rarr; FTK Imager &rarr; dc3dd &rarr; read the log), Case 02 + the log exercises under **YOUR TURN**, a **CHEAT SHEET authored** (`P05` had none), Task relabelled. As on `P03`, the regroup improved order: *&ldquo;what verified actually covers&rdquo;* now precedes the demo that produces a verified log. 31 &rarr; **33 screens, 6 dividers**. Both gates green &mdash; but only after trimming. | **`P05` is 145 topic-minutes; the next-largest page is ~90.** Adding the mandatory cheat sheet pushed it to 4126 visible words against the 4000 cap, and no single screen was the culprit &mdash; the counts are evenly spread (max 234, average ~125, all healthy). The page fails the *total* cap because it is **long, not dense**: it is really two pages of content under one number. Rather than weaken the gate, I trimmed ~130 words of real redundancy and elaboration (a duplicated destination-log note, tightened limitation prose, my own verbose dividers), landing at **3998 / 4000** &mdash; which is itself the finding: `P05` has no headroom. **Recommendation for Ebrahim: split `P05` into `T08` and `T09` as two pages.** Deferred here because it renumbers `P06`&ndash;`P14` and every cross-reference, homepage card, roadmap row and topic-map entry &mdash; a dedicated, gated step, not a side effect of the review of one page. |
| 2026-09-10 | **D149** | **`P06` retrofitted to shape-v2 &mdash; a light one, because its theory was already grouped.** Single topic (`T10`, hidden information) built with both parts of theory ahead of the lab, so no reorder was needed: the practical dividers were relabelled to **INSTRUCTOR DEMO** (the seven-file `EVS-10` sweep) and **YOUR TURN** (Case 03 and the six-sentences exercise), a **CHEAT SHEET was authored** (metadata / three ways to hide / four checks in cost order / detection proves presence not intent), and the closing screens reordered so the graded **TASK** comes before the report screen. 26 &rarr; **28 screens, 6 dividers**. Both gates green, and with room to spare (3161 / 4000) &mdash; the opposite of `P05`. | Worth recording as the pattern for the remaining pages: **a page whose two theory PARTs already sit before its lab needs only relabelling, a cheat sheet, and the task/report reorder** &mdash; the expensive part (regrouping interleaved theory) is only for the pages that interleave. `P06` also confirms the visual bar the rest of the pass holds to: it keeps its two rendered-from-evidence comparison images (the redaction that survived in the thumbnail, `D130`), which are *findings rendered*, not decoration &mdash; exactly the kind of real image the brief asks for and the only kind `D93`/`D94` allow. |
| 2026-09-10 | **D150** | **`P07` retrofitted to shape-v2 (light), and the density gate again caught a translated term in the new cheat sheet.** Single topic (`T11`, malicious files), theory already grouped into two PARTs before the lab, so: practical dividers relabelled to **INSTRUCTOR DEMO** (take one document and one binary apart without running either) and **YOUR TURN** (the paper exercises), a **CHEAT SHEET authored** whose spine is the page&rsquo;s own rule &mdash; *every over-claim is one adjective past the evidence* &mdash; and the closing screens reordered to put the graded **TASK** before the report screen. 27 &rarr; **29 screens, 6 dividers**. Both gates green after one fix. | The cheat&rsquo;s PE-layout line read *&ldquo;ترويسة MZ &hellip; الترويسة الاختيارية&rdquo;* &mdash; `ترويسة`, the translated form of *header*, which `D76` bans in favour of the English term. The gate flagged both instances; the fix put the whole PE sequence in English (`MZ header &rarr; PE signature &rarr; optional header`), which is how the body screens already write it. **Third page running where the density gate caught the reviewer&rsquo;s own new Arabic** (`الهوية` on `P03`, nothing on `P04`&ndash;`P06`, `ترويسة` here) &mdash; the cheat sheets are new prose written fast, and they are exactly where a translated term slips in, so every one is gated before it ships. |
| 2026-09-10 | **D151** | **`P08` regrouped to shape-v2, and the two held HDD photographs turned out already published on it.** Two topics (`T12` storage + `T13` partitioning), interleaved in v1 (theory &rarr; slack lab &rarr; theory &rarr; Case 04): now grouped &mdash; **PART 1 + PART 2** theory, then the slack lab as **INSTRUCTOR DEMO** and the whole of Case 04 (brief &rarr; table recovery &rarr; exercise &rarr; check) kept intact under **YOUR TURN**, a **CHEAT SHEET** authored (platter vs NAND, sector&rarr;cluster&rarr;slack, MBR/GPT, and the `BPB_BkBootSec = 6` recovery tell), Task relabelled. 24 &rarr; **26 screens, 6 dividers**. Both gates green. | Two records. **(a) The mapping stayed faithful to the original pedagogy:** the v1 *Lab* divider (slack) was the taught one and the v1 *On your own* divider (Case 04) was the independent one, so slack became the demo and Case 04 the your-turn &mdash; the labels changed, the intent did not, and Case 04 was **not** broken across the demo/your-turn line the way a mechanical split would have. **(b) `image_sources.md` is stale:** it lists the two Eric-Gaba HDD photos as *&ldquo;HELD, not published&rdquo;* for `P08`, but they are in fact on the *Platters and NAND* screen with figcaptions and their attribution &mdash; published, correctly, since the build. The rows exist (`D93` is satisfied); only the prose note is wrong. Flagged for the final course-wide pass, where the image manifest is reconciled against what the pages actually render. |
| 2026-09-10 | **D152** | **`P09` regrouped to shape-v2, and with it every v1 page (`P01`&ndash;`P09`) now matches the canonical shape.** Three topics (FAT, NTFS, carving): theory grouped into **PART 1 / 2 / 3**, the `$MFT` lab as **INSTRUCTOR DEMO**, all of Case 05 (brief &rarr; carve &rarr; exercise &rarr; check) kept intact under **YOUR TURN**, a **CHEAT SHEET** authored (three FAT structures, `0xE5` delete, resident vs non-resident `$DATA`, carving ignores the file system), Task relabelled &mdash; **seven dividers**, matching the three-topic `P11`/`P13`. 29 &rarr; **31 screens**. Both gates green after the by-now-routine header-term fix. | The retrofit half of the review is complete: `P01`&ndash;`P09` were each brought from the interleaved or cheat-less v1 shape to the grouped v2 one, every page re-gated against today&rsquo;s stricter density check, and three pages (`P03`, `P05`, `P09`) had a real teaching-order bug fixed *by* the regroup &mdash; a demo that ran before the theory that defines it. The count that matters: **nine cheat sheets authored where there were none or where the cheat was buried in a summary**, each the one screen a student keeps open in the next lab. What remains is `P10`&ndash;`P14`, which are already v2 &mdash; there the review is polish (visual density, simplification, lab-step verification against the real tools), not restructure. |
| 2026-09-10 | **D153** | **`P10`&ndash;`P14` were reviewed against all seven dimensions and need no rebuild &mdash; they were built last, in shape-v2, with every lesson already applied.** Each was re-verified against **today&rsquo;s** gates, not the build-day ones: density ALL PASS on all five (totals 2350&ndash;3075, worst single screens 233&ndash;250, zero translated terms, zero script-glue, part-dividers == is-part on every page), and render **zero findings** across all five widths on all five pages. Each carries the v2 essentials confirmed by inspection: a standalone **CHEAT SHEET** screen with real content (169&ndash;215 words, list or table), a **TASK** with grading, and a **Where we are** bridge that names the prior page. Their labs are already real: RegRipper 3.0, `libscca`, `python-registry`, `python-evtx` and `tshark` were run on the real `EVS-12/13/14` and `EVS-08`, and the SIMSCREENs quote that output verbatim (`D137`&ndash;`D142`). | The review&rsquo;s honest finding for the last five pages is *confirmation, not correction* &mdash; and saying so is more useful than manufacturing changes to look busy. The one number to watch: **`P13`&rsquo;s worst screen is exactly 250**, at the cap with no margin; it passes, but any future tightening of the density gate would catch it first. Left as-is rather than shaved by one word, because a real trim needs a reason beyond the gate. What the last five pages confirm is that the v2 shape and the evidence-first discipline, once the build had them, produced pages the review does not have to touch &mdash; which is the strongest evidence that the retrofit of `P01`&ndash;`P09` brought them to the same bar. |
| 2026-09-10 | **D154** | **The review-and-improve pass is COMPLETE: all 14 pages are shape-v2 and green on every gate, and the course-wide docs are reconciled.** `P01`&ndash;`P09` were retrofitted from v1 (grouped theory, INSTRUCTOR DEMO / YOUR TURN split, nine cheat sheets authored, graded TASK, references-last); `P10`&ndash;`P14` were verified and needed no rebuild. Every page passes `density_gate.py` against today&rsquo;s stricter limits and `render_gate.js` at 1400/1100/900/700/480 with zero findings; `order_gate.py` on `topic_map.md` PASSES. Course-wide fixes: `order_gate` `EVS_READY` completed with the four sets built during the build (`EVS-11/12/13/14`), `image_sources.md` corrected (the HDD photos are published on `P08`, not held; `invoice_compare.png` marked a spare), all 14 cover labels aligned to *Page N of 14*, and the five published-photo hashes re-verified (5/5 OK). | The pass did what a review should: it did not just re-shape pages, it **found real defects the build and the old gate had passed** &mdash; a wrong term (`cluster` for a set of files on `P02`), three teaching-order bugs fixed by regrouping (`P03`, `P05`, `P09`), an over-dense screen that no longer met the tightened cap (`P04`), translated terms in the reviewer&rsquo;s own new cheat sheets (`P03`, `P07`, `P09`), and two stale documents. **One open decision is deliberately left to Ebrahim:** `P05` is 145 minutes &mdash; the one genuinely oversized page &mdash; and splitting it into `T08`/`T09` renumbers the whole diploma, so it is his call, not a side effect of the review. The 154 decisions of this project all serve the same end the capstone names: a course another instructor could pick up and teach, and no student could be misled by. || 2026-09-10 | **D155** | **Instructor teaching guides authored for P04-P06 (the AUTHOR-from-scratch pages) and committed to instructor/page-NN/.** These three had no packages/ script (unlike P01-P03), so each guide was written from the student page beats + build_log + the verified free-lab maps: RTL light printable HTML on the shared instructor.css, structured prep -> clock -> part-by-part say-boxes -> lab step cards -> report -> free task -> Q&A. Free-lab decisions recorded per page. | The load-bearing free-lab finding: **no free lab (TryHackMe or CyberDefenders) teaches ACQUISITION as a decision** - every CyberDefenders lab opens "you have been provided with an image", and THM memoryacquisition (the only judgement one) is premium. So P05 keeps the student-owned-USB acquisition as primary (ground truth beats realism) + THM Forensic Imaging (free) as the structured supplement. P06 has real in-hand evidence (EVS-10, no blocker) so the seven-file sweep IS the free lab; picoCTF Forensics (free forever, browser) is the optional external. P04 = THM Volatility Essentials (free) + CyberDefenders Reveal (free) with the M1/PEB caveat. Every guide render-clean (0 pageerrors) and footer-links the matching student page; the instructor/index.html landing page is still pending (task #50). |
| 2026-09-10 | **D156** | **All 14 instructor teaching guides + the instructor landing page are authored, gated and committed to instructor/.** P01-P03 were built from the existing packages/ scripts; P04-P14 were AUTHORED from scratch (student-page beats + build_log + verified free-lab maps) since only P01-P03 had instructor markdown. Each guide is RTL light printable HTML on one shared instructor.css, structured: prep -> clock -> part-by-part say-boxes -> lab step-cards (do/say/breaks) -> report -> free-lab task -> Q&A. instructor/index.html is a 14-card landing page (hero, prep-checklist, per-page free-lab). Every file render-clean (0 pageerrors); item 6 (the most-important ask) is complete. | Free-lab task assigned per page, all verified-free with a report (item 3 satisfied course-wide): P01 NIST SP 800-86 reading; P02/P05 THM Forensic Imaging; P03 Gary Kessler+ExifTool; P04 THM Volatility Essentials+CD Reveal; P06 picoCTF; P07 CD Obfuscated; P08 THM MBR/GPT+FAT32; P09 THM FAT32 (carving stays on free foremost/photorec since THM filecarving+NTFS Analysis are premium); P10 THM Windows Forensics 1; P11 CD Sysinternals (taught WITH the ShimCache presence-not-execution caveat the lab omits); P12 THM Compromised Windows Analysis; P13 CD PacketMaze; P14 THM IR Timeline Analysis. Pedagogical caveats preserved in the guides: M1/PEB (cmdline is a lead), ShimCache records presence, mp2 timestamp-above-GUID, first-install-is-latest-date, verified-neq-complete-neq-correct, one-verb-per-artifact, one-adjective-past-the-bytes. |
| 2026-09-10 | **D157** | **Homepage stats corrected, course-wide Arabic swept, README updated, and the repo verified clean for GitHub.** Homepage hero fixed to the true counts: **25 Topics** (was 62; matches the body "25 topics on 14 pages" and T01-T25) and **10 Evidence sets** (was 9; the built sets EVS-01/05/07/08/09/10/11/12/13/14 — EVS-02/03/04/06 are still PENDING, exactly Ebrahim's own count). One translated technical term swept from the new guides (P01 الآثار -> الـ artifacts, D132/D76). README updated for the 14-page + instructor structure (new instructor/ row, page pointer, current gates). | Final verification: all 15 instructor files render-clean (0 pageerrors); all 14 topic pages PASS density_gate; order_gate PASS; no banned translated term remains in any instructor guide; no evidence bytes (.dd/.E01/.pcap/hives) are committable (.gitignore + a clean untracked tree of 80 legitimate files, zero scratch). The five top-level meta files (00_INSTRUCTIONS, ECDFP_CLAUDE_SETUP, PROJECT, DECISIONS, README) are already tracked/public from before — left as-is, not removed. The repo is ready to push; the commit/push itself is left to Ebrahim (his GitHub remote/credentials). |
| 2026-09-10 | **D158** | **Report-section numbering unified to the canonical 12-section template across the course.** The rubric page and design/report_template.md (declared canonical) number the report 7 Acquisition · 8 Findings · 9 Interpretation · 10 Limitations · 11 Conclusions · 12 Appendices, and P01/P03 already followed it — but P06, P07, P09, P10, P11, P12, P13 and P14 still said **Section 7 = Findings** (and P14 said **Section 8 = Timeline**, **9 = conclude**), the pre-migration 10-section numbering. Every such reference (EN + AR, including P14 SIMSCREEN strings) was renumbered 7→8 for Findings; P14 now files the super-timeline as Appendix F under §12 (where the template assigns T24) and reads it from §8. Same fix applied to the P06/P11/P14 instructor guides. All 8 pages re-pass density_gate; P14 and P06 render clean with their real JS. | Found while building the worked report example (report/example.html), which follows the canonical numbering and would otherwise have contradicted eight pages — a student following P10's "write Section 7" would have been writing Findings into the Acquisition slot and failing criterion 3 on the rubric that grades them. **One pedagogical tension is deliberately left for Ebrahim, not silently resolved:** P14 teaches a compound finding (claim + artifact + confidence + limit) while the rubric grades §8 as fact-only with confidence in §9 — the numbering is now consistent, but the capstone wording still folds confidence into the finding line. |
| 2026-09-10 | **D159** | **Homepage reorganised, the lab consolidated to one page with a real topology diagram, the three dead Resources placeholders replaced by real pages, and a full worked report example published.** (a) docs/index.html: the 14-page grid was literally placed *after the site footer*; it now follows the hero, then distinction → lab → evidence → records → resources → about → footer. (b) lab/index.html + lab/setup.html merged into one page (topology · machines · before-first-session · Build FOR-WS01 A/B/C · snapshots · troubleshooting); setup.html is a redirect stub so no bookmark breaks; a new SVG topology (host boundary, three colour-coded VMs on the host-only bus, the one-way evidence pipeline as four numbered steps, the internet shown blocked) is embedded on both the lab page and the homepage lab section. (c) Practice labs → resources/labs.html (14 free labs, platform-badged, each with its hand-in and caveats); Glossary → resources/glossary.html (≈45 terms, each with PROVES / DOES NOT PROVE in the course colours); Milestone review removed (no content existed behind it); Cheat sheets now links to the pages. (d) report/example.html: the 12-section report written in full for a fictional departing-employee USB case (NBL-2026-014), a teaching note (EN + AR) on every section, the three-box discipline in §8/§9/§10, a declared custody gap, and the honest §11 answer *not established* for the copy question because the USB device was never recovered. Stale text fixed en route (nine→ten evidence sets). 101 local links checked, 0 missing; order_gate PASS. | Two build-vs-run facts were made explicit rather than left implied: the FOR-WS01 build needs NAT (Windows Update, tool downloads) and Part C now switches the adapter to Host-only *before* the CLEAN-TOOLS snapshot, which is what makes the "nothing leaves the host" topology true. The report example follows the canonical §8-Findings numbering, which is what surfaced D158. Its realism is grounded in the standard Windows USB artifact chain (USBSTOR → Properties 0064/0066/0067 → MountedDevices → MountPoints2 → LNK/JumpList → Prefetch → $MFT/$I) and in what each artifact does *not* prove — the example never names a person as an actor, per the template's own rule. |
| 2026-09-10 | **D160** | **Release gate for the 2026-09-10 push: run on the committable set, findings classified, commit approved by Ebrahim.** The PowerShell gate cannot run in the Cowork VM, so an identical scan (same regexes, skip-dirs, allow-list, stage phrases, 20 MB cap) was run in Python over exactly the tracked + untracked-not-ignored files (335). Result: 0 secrets, 0 evidence bytes, 0 oversized, 0 stage directions; `check_links` equivalent 470/470 local refs resolve. 139 PII-pattern hits, all classified: 62 "phone numbers" = byte/sector counts and hex; 72 private IPs = RFC 1918 lab addresses (the allow-list only covers RFC 5737, and the same class is already public in `knowledge_base/`); 5 emails = one public Debian list address already public + the fictional EVS-08 case (`billing@invoices-cloud.net`, `l.bennett@company.com`). Committed with explicit paths, never `-A`; push handed to GitHub Desktop (R11). Commit hashes: see D161 — the history was rebuilt once before it stood. | Same precedent as the first push (2026-09-06: "226 PII candidates all false positives"). **Two follow-ups, not done inside the publish run per the skill:** (1) tune `precommit_scan.ps1` — add the RFC 1918 ranges and the course's fictional domains to `$piiAllow`, and tighten the phone regex so byte counts stop matching, so the official gate prints CLEAN on this tree; (2) the two fictional domains are not RFC 2606 reserved (`company.com` is real) — changing them cascades into the EVS-08 generator, pcap bytes and manifest hashes, so it is a deliberate evidence-set decision, not a text edit. **Mount note:** on this VM every git command that takes-and-rolls-back a lock leaves a stale `.lock` (`git status`, `git reset`); they were moved to `_to_delete/` (9 files) and 279 orphan `tmp_obj_*` files sit in `.git/objects/` — harmless to git, to be removed from Windows. |
| 2026-09-10 | **D161** | **`instructor/` is private teaching material and is excluded from the public repository; the history was rebuilt so it never appears.** Ebrahim's instruction on seeing the commit list: the instructor guides are his, not part of the published course. `/instructor/` added to `.gitignore` beside the answer keys (the existing "instructor copy only" section); README and PROJECT.md no longer reference the folder; the guides stay on disk at `instructor/` and are opened locally. `packages/` (the earlier instructor markdown, already public by prior decision) was left as it was — his call whether that follows. | **Incident, recorded plainly.** The first push of the day went out from GitHub Desktop at 10:24:03 (`update by push`, `origin/main` → `0f2086c`) and included the instructor commit `68cb603` — about a minute before the instruction to hold. Exposure: repository source only (GitHub Pages serves `docs/`, so nothing reached the live site); no credentials or personal data were involved. Remedy: local `main` reset to `e73d2f9`, the commits rebuilt from the same worktree without `instructor/`, and the rewritten branch **force-pushed** from GitHub Desktop to replace the remote history. GitHub can keep unreachable commits fetchable by SHA for a time after a force push; if that matters, the options are a GitHub Support purge request or recreating the repository. Lesson for the skill: the hand-off message must say *do not push yet* explicitly when a review is still open — a list of ready commits reads as an invitation to push. |
| 2026-09-10 | **D162** | **Every local CSS/JS reference under `docs/` is cache-busted with a content hash (`?v=<sha256[:8]>`), applied by the new idempotent `scripts/stamp_assets.py`, which is run before every publish.** 155 references across 30 pages. `tools\check_links.ps1` already strips `?query` before resolving, so the link gate is unaffected; `file://` loads still work. | **Root cause of the black lifecycle figure Ebrahim screenshotted after the push:** the site had been live before at the very same asset URLs, and GitHub Pages serves assets with a ten-minute cache. His browser paired the freshly fetched new page HTML with a *cached old* `ecdfp.css` — which has none of the 45 `.dgm` rules the stepped figures need — giving black boxes, unstyled Prev/Play/Next controls and "WHERE WE AREORIENTATION" run together. Reproduced by diffing `e73d2f9:docs/assets/css/ecdfp.css` against the pushed one; the page renders correctly locally. A content-hash query string makes a changed asset a new URL, so this class of bug cannot recur on any future deploy. Immediate workaround was a hard refresh (Ctrl+F5). |
| 2026-09-10 | **D163** | **The page-level word total in `density_gate.py` is retired; the per-screen limits are the gate.** The old `total visible words <= 4000` check was replaced by `total <= 180 × screens` — the same average the gate already enforces — so a page is free to grow by adding screens, never by thickening them. Unchanged: average ≤ 180 words/screen, worst single screen ≤ 250, a visual on every screen, ≤ 3 consecutive paragraphs, MCQ contract, sidebar == screens, and every bilingual check (`D76`, `D126`, `D133`). The gate now also prints the *id* of the worst screen. | Ebrahim's review of P01/P02 and the Session 2/3 material asked for far more content on P03, P08 and P09 (they went to 40, 40 and 55 screens); a fixed page cap would have forced either trimming teaching that the instructor explicitly wanted, or splitting pages and renumbering the diploma. The per-screen limits are the ones that protect the student; the page total was only ever a proxy for them. |
| 2026-09-10 | **D164** | **Knowledge checks are real MCQs: `quiz.js` rewritten to drive both the original list markup (`.opts li[data-correct][data-why]`) and the new block markup (`div.q.mcq[data-correct] > label > input` + `.feedback`); the cheat sheet prints as one screen.** Clicking marks `answered`/`is-right`/`is-wrong` and prefixes the feedback with *Correct.* or *Not quite — the answer is x.*; the feedback is hidden until answered (`ecdfp.css` §30). Printing: `session.js` listens to `beforeprint`/`afterprint`, tags the open screen `.print-target` and sets `body.print-one`, and §30 hides every other screen and the chrome, black on white. | Ebrahim's screenshots showed two defects on every page: the MCQ options did nothing (the markup on the pages never matched what `quiz.js` was reading) and the answer text was visible before any click, so the knowledge checks tested nothing; and *Ctrl+P* on a cheat sheet printed the whole deck, dark, over dozens of pages. Both fixed globally — one JS file, one CSS section — so every page inherits the fix, and verified with a printed one-page PDF. |
| 2026-09-10 | **D165** | **Small task packs and practice files are published on the site under `docs/downloads/` with their SHA-256 on the page; the student downloads with `Invoke-WebRequest`, verifies with `Get-FileHash`, then `Expand-Archive`.** Three packs: `p01-task.zip` (README, the report template, the artifact line — 12 KB), `p03-practice.zip` (File09 · File10.png · File11.docx · File12.mp4 from the previous course — 5.1 MB) and `p03-task.zip` (five extension-less files from the *File signature evidence* set, 1.7 MB). Zip timestamps are fixed so a rebuild is byte-identical. This narrows `R9`'s *never commit evidence bytes* to **evidence images**: teaching files of a few MB, whose provenance and licence are recorded in `image_sources.md`, are published; disk/memory images, hives and pcaps are not. | Ebrahim's instruction: the student must get the task as files from the site — *"يحملوه من الموقع أسهل"* — and must never see a reference to a `.md` file in a package folder (`homework.md`, `student_activity.md`); every such reference was replaced by content on the screen. The same download-verify-expand sequence is the course's own habit for every exhibit, so the pack is also the first place the student practises it. **One file was scrubbed before publishing:** `File11.docx` carried a real person's name and a real company in its OOXML core properties and inside the embedded OLE `.doc`; both were replaced by fictional values of identical length (ASCII and UTF-16LE) so the OLE offsets stay valid, and the pack hash was taken after the scrub. |
| 2026-09-10 | **D166** | **Screenshots from the four TryHackMe rooms Ebrahim named (MBR and GPT Analysis, FAT32 Analysis, NTFS Analysis, Ext Analysis) are used on P08/P09 as tool screenshots only, each with the caption *Screenshot: TryHackMe, room X*, and every THM diagram was redrawn as our own stepped SVG.** 16 captures published under `docs/assets/img/fs/` (HxD Data Inspector and Go-to, Disk Management, msinfo32, colour-coded MBR and GPT header, the six GPT entries, FTK Imager on `$MFT`/`$I30`/system files, Timeline Explorer); 13 more (MFTECmd runs, `$Extend`, `$J` export, the three-pane MBR) kept for the instructor guides only. The raw *File system photos* folder stays untracked; `image_sources.md` gained a section with a row per file. | Ebrahim gave the images and the rooms — *"استخدم الصور دي أو اعمل زيها"* — and his Chrome session was used to read the rooms. The line drawn: a **tool's real output** is evidence of what the tool shows and is quoted with its source, exactly as the course quotes `exiftool` or `fls`; a **teaching diagram** is the room's authorship, so the concept was rebuilt in the course's own figure language (`dgm.js` steps, Arabic layer) rather than copied. Every screenshot was cropped, resized ≤ 1400 px and quantized; none shows a THM username or session. |
| 2026-09-10 | **D167** | **P01, P02, P03, P08 and P09 rebuilt against Ebrahim's screenshot review, `Resources/Instructor/Session 2.pdf` and `Session 3.pdf`, and the four THM rooms; P09 gains PART 4 — ext.** P01 (30 screens): the twelve sections as a table, a *how the report fills* figure, the toolkit divider routing to the lab build page and `verify_tools.ps1 → Missing: 0`, both exercises inline, the task as verify → download → hash → expand. P02 (31): the registry key in the software write-block figure made visible (`.wu code` rule), three MCQ screens, the homework inlined. P03 (40): bit→nibble→byte→hex ladder with the 0–127 table, twelve-format header/footer table, delete preview, Properties, endianness, the two EXIF clocks on a real Nikon file, the four-file signature demo, the five-file task with the *find the city* question (answer held in the instructor guide). P08 (40): inside a hard disk, disk/partition/volume, boot, a real MBR byte by byte, the LBA arithmetic, the type byte, protective MBR, GPT header and entry with the mixed-endian GUID, the backup GPT, the TestDisk 7.2 lab. P09 (55): FAT32 volume map and BPB, the chain before/after, three ATT&CK techniques on FAT32, NTFS system files, the MFT record, MACB ×2, the two journals with `$J` reason codes, `$I30` slack, the collect-once/parse-three MFTECmd lab, PhotoRec, and seven ext screens with a Kali lab the student builds himself (`mkfs.ext4 -b 4096`, `fls`, `istat`, `strings -t d` → `dd`, `debugfs lsdel`). All five pass `density_gate.py` and `render_gate.js` at five widths. | Ebrahim's brief for Session 3 was *"لازم تركز فيه أوي"* and for the material *"أقوى ماتيريال ممكنة"* — everything the previous course explained live is now on the student page, and only the answers, the live-demo choreography and the unused captures are private. Two facts were verified rather than copied: `MFTECmd` does not parse `$LogFile` (stated on P09 as a caveat with the alternatives), and a 32 MB ext4 image defaults to 1 024-byte blocks, so the lab forces `-b 4096` and creates `normal.txt` first so it lands on inode 12 as the page says. ext was a spoken aside in the previous course; it is now a part of the page because the THM room and the Kali VM the students already have make it a real lab. |
| 2026-09-10 | **D168** | **The instructor guides for P01, P02, P03, P08 and P09 gained a section 8 — *نسخة 2026-09-10* — with the screen map of the rebuilt page, the per-screen script, every MCQ answer, the task answer keys (the practice set, the five-file identification, the city — Arezzo, from `43.46716 N 11.88539 E`), the TestDisk / MFTECmd / PhotoRec / ext lab expectations, and the instructor-only captures; the whole `instructor/` tree is restored on disk and stays gitignored.** | The folder had gone from the working tree after the history rebuild (`D161`); it was rebuilt from the session's copy and extended, and is never in a commit — `git ls-tree origin/main` and local `HEAD` were both checked to contain zero `instructor/` paths before this session's commit. Answers live only here: the student pages ask the questions and hold none of the keys. |
